/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.credentials;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;
import resources.credentials.requests.CreateCredentialRequest;
import resources.credentials.requests.DeleteCredentialRequest;
import resources.credentials.requests.ListCredentialsRequest;
import resources.credentials.requests.UpdateCredentialRequest;
import resources.credentials.types.CreateCredentialResponse;
import resources.credentials.types.ListCredentialsResponse;
import resources.credentials.types.UpdateCredentialResponse;

public class AsyncRawCredentialsClient {
  protected final ClientOptions clientOptions;

  public AsyncRawCredentialsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Create SIP credentials for trunk authentication.
   */
  public CompletableFuture<VobizApiHttpResponse<CreateCredentialResponse>> createCredential(
      String authId, CreateCredentialRequest request) {
    return createCredential(authId,request,null);
  }

  /**
   * Create SIP credentials for trunk authentication.
   */
  public CompletableFuture<VobizApiHttpResponse<CreateCredentialResponse>> createCredential(
      String authId, CreateCredentialRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("credentials");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      RequestBody body;
      try {
        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
      }
      catch(JsonProcessingException e) {
        throw new VobizApiException("Failed to serialize request", e);
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("POST", body)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Content-Type", "application/json")
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      CompletableFuture<VobizApiHttpResponse<CreateCredentialResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateCredentialResponse.class), response));
              return;
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (IOException e) {
            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Retrieve all SIP credentials on the account.
     */
    public CompletableFuture<VobizApiHttpResponse<ListCredentialsResponse>> listCredentials(
        String authId) {
      return listCredentials(authId,ListCredentialsRequest.builder().build());
    }

    /**
     * Retrieve all SIP credentials on the account.
     */
    public CompletableFuture<VobizApiHttpResponse<ListCredentialsResponse>> listCredentials(
        String authId, RequestOptions requestOptions) {
      return listCredentials(authId,ListCredentialsRequest.builder().build(),requestOptions);
    }

    /**
     * Retrieve all SIP credentials on the account.
     */
    public CompletableFuture<VobizApiHttpResponse<ListCredentialsResponse>> listCredentials(
        String authId, ListCredentialsRequest request) {
      return listCredentials(authId,request,null);
    }

    /**
     * Retrieve all SIP credentials on the account.
     */
    public CompletableFuture<VobizApiHttpResponse<ListCredentialsResponse>> listCredentials(
        String authId, ListCredentialsRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("trunks")
        .addPathSegments("credentials");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<VobizApiHttpResponse<ListCredentialsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCredentialsResponse.class), response));
                return;
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (IOException e) {
              future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Update the password for an existing SIP credential.
       */
      public CompletableFuture<VobizApiHttpResponse<UpdateCredentialResponse>> updateCredential(
          String authId, String credentialId, UpdateCredentialRequest request) {
        return updateCredential(authId,credentialId,request,null);
      }

      /**
       * Update the password for an existing SIP credential.
       */
      public CompletableFuture<VobizApiHttpResponse<UpdateCredentialResponse>> updateCredential(
          String authId, String credentialId, UpdateCredentialRequest request,
          RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/Account")
          .addPathSegment(authId)
          .addPathSegments("credentials")
          .addPathSegment(credentialId);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new VobizApiException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("PUT", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          CompletableFuture<VobizApiHttpResponse<UpdateCredentialResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateCredentialResponse.class), response));
                  return;
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (IOException e) {
                future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Delete an existing SIP credential.
         */
        public CompletableFuture<VobizApiHttpResponse<Optional<String>>> deleteCredential(
            String authId, String credentialId) {
          return deleteCredential(authId,credentialId,DeleteCredentialRequest.builder().build());
        }

        /**
         * Delete an existing SIP credential.
         */
        public CompletableFuture<VobizApiHttpResponse<Optional<String>>> deleteCredential(
            String authId, String credentialId, RequestOptions requestOptions) {
          return deleteCredential(authId,credentialId,DeleteCredentialRequest.builder().build(),requestOptions);
        }

        /**
         * Delete an existing SIP credential.
         */
        public CompletableFuture<VobizApiHttpResponse<Optional<String>>> deleteCredential(
            String authId, String credentialId, DeleteCredentialRequest request) {
          return deleteCredential(authId,credentialId,request,null);
        }

        /**
         * Delete an existing SIP credential.
         */
        public CompletableFuture<VobizApiHttpResponse<Optional<String>>> deleteCredential(
            String authId, String credentialId, DeleteCredentialRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/Account")
            .addPathSegment(authId)
            .addPathSegments("credentials")
            .addPathSegment(credentialId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("DELETE", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            CompletableFuture<VobizApiHttpResponse<Optional<String>>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<Optional<String>>() {}), response));
                    return;
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (IOException e) {
                  future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
              }
            });
            return future;
          }
        }
