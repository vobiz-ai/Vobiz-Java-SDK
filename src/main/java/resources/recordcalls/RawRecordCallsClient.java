/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.recordcalls;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.lang.Void;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.recordcalls.requests.StartRecordingRequest;
import resources.recordcalls.requests.StopRecordingRequest;

public class RawRecordCallsClient {
  protected final ClientOptions clientOptions;

  public RawRecordCallsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Begin recording an active call. Set format, enable transcription, and configure a callback URL.
   */
  public VobizApiHttpResponse<Object> startRecording(String authId, String callUuid) {
    return startRecording(authId,callUuid,StartRecordingRequest.builder().build());
  }

  /**
   * Begin recording an active call. Set format, enable transcription, and configure a callback URL.
   */
  public VobizApiHttpResponse<Object> startRecording(String authId, String callUuid,
      RequestOptions requestOptions) {
    return startRecording(authId,callUuid,StartRecordingRequest.builder().build(),requestOptions);
  }

  /**
   * Begin recording an active call. Set format, enable transcription, and configure a callback URL.
   */
  public VobizApiHttpResponse<Object> startRecording(String authId, String callUuid,
      StartRecordingRequest request) {
    return startRecording(authId,callUuid,request,null);
  }

  /**
   * Begin recording an active call. Set format, enable transcription, and configure a callback URL.
   */
  public VobizApiHttpResponse<Object> startRecording(String authId, String callUuid,
      StartRecordingRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("Call")
      .addPathSegment(callUuid)
      .addPathSegments("Record");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      RequestBody body;
      try {
        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
      }
      catch(JsonProcessingException e) {
        throw new VobizApiException("Failed to serialize request", e);
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("POST", body)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Content-Type", "application/json")
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Stop an active recording on an in-progress call.
     */
    public VobizApiHttpResponse<Void> stopRecording(String authId, String callUuid) {
      return stopRecording(authId,callUuid,StopRecordingRequest.builder().build());
    }

    /**
     * Stop an active recording on an in-progress call.
     */
    public VobizApiHttpResponse<Void> stopRecording(String authId, String callUuid,
        RequestOptions requestOptions) {
      return stopRecording(authId,callUuid,StopRecordingRequest.builder().build(),requestOptions);
    }

    /**
     * Stop an active recording on an in-progress call.
     */
    public VobizApiHttpResponse<Void> stopRecording(String authId, String callUuid,
        StopRecordingRequest request) {
      return stopRecording(authId,callUuid,request,null);
    }

    /**
     * Stop an active recording on an in-progress call.
     */
    public VobizApiHttpResponse<Void> stopRecording(String authId, String callUuid,
        StopRecordingRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("Call")
        .addPathSegment(callUuid)
        .addPathSegments("Record");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("DELETE", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(null, response);
          }
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }
    }
