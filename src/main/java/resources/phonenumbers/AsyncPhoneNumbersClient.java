/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.phonenumbers;

import core.ClientOptions;
import core.RequestOptions;
import java.lang.Object;
import java.lang.String;
import java.lang.Void;
import java.util.concurrent.CompletableFuture;
import resources.phonenumbers.requests.AssignDidToSubaccountRequest;
import resources.phonenumbers.requests.AssignNumberToTrunkRequest;
import resources.phonenumbers.requests.CancelNumberReleaseRequest;
import resources.phonenumbers.requests.GetNumberHealthRequest;
import resources.phonenumbers.requests.ListInventoryNumbersRequest;
import resources.phonenumbers.requests.ListNumbersRequest;
import resources.phonenumbers.requests.PurchaseFromInventoryRequest;
import resources.phonenumbers.requests.UnassignDidFromSubaccountRequest;
import resources.phonenumbers.requests.UnassignNumberFromTrunkRequest;
import resources.phonenumbers.requests.UnrentNumberRequest;
import resources.phonenumbers.types.CancelNumberReleaseResponse;
import resources.phonenumbers.types.GetNumberHealthResponse;
import resources.phonenumbers.types.ListInventoryNumbersResponse;
import resources.phonenumbers.types.ListNumbersResponse;
import resources.phonenumbers.types.UnrentNumberResponse;

public class AsyncPhoneNumbersClient {
  protected final ClientOptions clientOptions;

  private final AsyncRawPhoneNumbersClient rawClient;

  public AsyncPhoneNumbersClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new AsyncRawPhoneNumbersClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public AsyncRawPhoneNumbersClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * List all phone numbers on your account.
   */
  public CompletableFuture<ListNumbersResponse> listNumbers(String authId) {
    return this.rawClient.listNumbers(authId).thenApply(response -> response.body());
  }

  /**
   * List all phone numbers on your account.
   */
  public CompletableFuture<ListNumbersResponse> listNumbers(String authId,
      RequestOptions requestOptions) {
    return this.rawClient.listNumbers(authId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * List all phone numbers on your account.
   */
  public CompletableFuture<ListNumbersResponse> listNumbers(String authId,
      ListNumbersRequest request) {
    return this.rawClient.listNumbers(authId, request).thenApply(response -> response.body());
  }

  /**
   * List all phone numbers on your account.
   */
  public CompletableFuture<ListNumbersResponse> listNumbers(String authId,
      ListNumbersRequest request, RequestOptions requestOptions) {
    return this.rawClient.listNumbers(authId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Release a phone number from your account. By default, the number enters
   * <code>pending_release</code> for a 24-hour cooldown. You can cancel the release during
   * that window. Set <code>immediate=true</code> to skip the cooldown; an immediate release
   * cannot be cancelled.
   */
  public CompletableFuture<UnrentNumberResponse> unrentNumber(String authId, String e164) {
    return this.rawClient.unrentNumber(authId, e164).thenApply(response -> response.body());
  }

  /**
   * Release a phone number from your account. By default, the number enters
   * <code>pending_release</code> for a 24-hour cooldown. You can cancel the release during
   * that window. Set <code>immediate=true</code> to skip the cooldown; an immediate release
   * cannot be cancelled.
   */
  public CompletableFuture<UnrentNumberResponse> unrentNumber(String authId, String e164,
      RequestOptions requestOptions) {
    return this.rawClient.unrentNumber(authId, e164, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Release a phone number from your account. By default, the number enters
   * <code>pending_release</code> for a 24-hour cooldown. You can cancel the release during
   * that window. Set <code>immediate=true</code> to skip the cooldown; an immediate release
   * cannot be cancelled.
   */
  public CompletableFuture<UnrentNumberResponse> unrentNumber(String authId, String e164,
      UnrentNumberRequest request) {
    return this.rawClient.unrentNumber(authId, e164, request).thenApply(response -> response.body());
  }

  /**
   * Release a phone number from your account. By default, the number enters
   * <code>pending_release</code> for a 24-hour cooldown. You can cancel the release during
   * that window. Set <code>immediate=true</code> to skip the cooldown; an immediate release
   * cannot be cancelled.
   */
  public CompletableFuture<UnrentNumberResponse> unrentNumber(String authId, String e164,
      UnrentNumberRequest request, RequestOptions requestOptions) {
    return this.rawClient.unrentNumber(authId, e164, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Cancel a pending number release during the 24-hour cooldown. The number is
   * restored to <code>active</code>, the cooldown timer is cleared, and the release fee is
   * refunded in full to the account balance. Any trunk or voice application
   * detached by the release is not re-attached automatically.
   */
  public CompletableFuture<CancelNumberReleaseResponse> cancelNumberRelease(String accountId,
      String e164) {
    return this.rawClient.cancelNumberRelease(accountId, e164).thenApply(response -> response.body());
  }

  /**
   * Cancel a pending number release during the 24-hour cooldown. The number is
   * restored to <code>active</code>, the cooldown timer is cleared, and the release fee is
   * refunded in full to the account balance. Any trunk or voice application
   * detached by the release is not re-attached automatically.
   */
  public CompletableFuture<CancelNumberReleaseResponse> cancelNumberRelease(String accountId,
      String e164, RequestOptions requestOptions) {
    return this.rawClient.cancelNumberRelease(accountId, e164, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Cancel a pending number release during the 24-hour cooldown. The number is
   * restored to <code>active</code>, the cooldown timer is cleared, and the release fee is
   * refunded in full to the account balance. Any trunk or voice application
   * detached by the release is not re-attached automatically.
   */
  public CompletableFuture<CancelNumberReleaseResponse> cancelNumberRelease(String accountId,
      String e164, CancelNumberReleaseRequest request) {
    return this.rawClient.cancelNumberRelease(accountId, e164, request).thenApply(response -> response.body());
  }

  /**
   * Cancel a pending number release during the 24-hour cooldown. The number is
   * restored to <code>active</code>, the cooldown timer is cleared, and the release fee is
   * refunded in full to the account balance. Any trunk or voice application
   * detached by the release is not re-attached automatically.
   */
  public CompletableFuture<CancelNumberReleaseResponse> cancelNumberRelease(String accountId,
      String e164, CancelNumberReleaseRequest request, RequestOptions requestOptions) {
    return this.rawClient.cancelNumberRelease(accountId, e164, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Browse available phone numbers in inventory that are not assigned to
   * any account. Only numbers with <code>status='active'</code> and <code>auth_id=NULL</code>
   * are returned. These numbers are ready to be purchased.
   */
  public CompletableFuture<ListInventoryNumbersResponse> listInventoryNumbers(String authId) {
    return this.rawClient.listInventoryNumbers(authId).thenApply(response -> response.body());
  }

  /**
   * Browse available phone numbers in inventory that are not assigned to
   * any account. Only numbers with <code>status='active'</code> and <code>auth_id=NULL</code>
   * are returned. These numbers are ready to be purchased.
   */
  public CompletableFuture<ListInventoryNumbersResponse> listInventoryNumbers(String authId,
      RequestOptions requestOptions) {
    return this.rawClient.listInventoryNumbers(authId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Browse available phone numbers in inventory that are not assigned to
   * any account. Only numbers with <code>status='active'</code> and <code>auth_id=NULL</code>
   * are returned. These numbers are ready to be purchased.
   */
  public CompletableFuture<ListInventoryNumbersResponse> listInventoryNumbers(String authId,
      ListInventoryNumbersRequest request) {
    return this.rawClient.listInventoryNumbers(authId, request).thenApply(response -> response.body());
  }

  /**
   * Browse available phone numbers in inventory that are not assigned to
   * any account. Only numbers with <code>status='active'</code> and <code>auth_id=NULL</code>
   * are returned. These numbers are ready to be purchased.
   */
  public CompletableFuture<ListInventoryNumbersResponse> listInventoryNumbers(String authId,
      ListInventoryNumbersRequest request, RequestOptions requestOptions) {
    return this.rawClient.listInventoryNumbers(authId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Purchase a phone number from inventory and assign it to your account.
   * Debits your account balance for the setup fee and monthly fee. For
   * sub-accounts (SA_), the parent master account (MA_) is charged.
   */
  public CompletableFuture<Object> purchaseFromInventory(String authId,
      PurchaseFromInventoryRequest request) {
    return this.rawClient.purchaseFromInventory(authId, request).thenApply(response -> response.body());
  }

  /**
   * Purchase a phone number from inventory and assign it to your account.
   * Debits your account balance for the setup fee and monthly fee. For
   * sub-accounts (SA_), the parent master account (MA_) is charged.
   */
  public CompletableFuture<Object> purchaseFromInventory(String authId,
      PurchaseFromInventoryRequest request, RequestOptions requestOptions) {
    return this.rawClient.purchaseFromInventory(authId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Assign a phone number to a specific SIP trunk. Once assigned, all
   * inbound calls to that phone number will be routed through the
   * designated trunk. The phone number must be URL-encoded; use <code>%2B</code>
   * instead of <code>+</code> (e.g., <code>%2B912271264217</code>).
   */
  public CompletableFuture<Void> assignNumberToTrunk(String authId, String phoneNumber,
      AssignNumberToTrunkRequest request) {
    return this.rawClient.assignNumberToTrunk(authId, phoneNumber, request).thenApply(response -> response.body());
  }

  /**
   * Assign a phone number to a specific SIP trunk. Once assigned, all
   * inbound calls to that phone number will be routed through the
   * designated trunk. The phone number must be URL-encoded; use <code>%2B</code>
   * instead of <code>+</code> (e.g., <code>%2B912271264217</code>).
   */
  public CompletableFuture<Void> assignNumberToTrunk(String authId, String phoneNumber,
      AssignNumberToTrunkRequest request, RequestOptions requestOptions) {
    return this.rawClient.assignNumberToTrunk(authId, phoneNumber, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Remove the assignment between a phone number and a SIP trunk. After
   * unassignment, the number remains in your account inventory but will
   * no longer route inbound calls through the previously assigned trunk.
   * URL-encode the phone number (use <code>%2B</code> instead of <code>+</code>).
   */
  public CompletableFuture<Void> unassignNumberFromTrunk(String authId, String phoneNumber) {
    return this.rawClient.unassignNumberFromTrunk(authId, phoneNumber).thenApply(response -> response.body());
  }

  /**
   * Remove the assignment between a phone number and a SIP trunk. After
   * unassignment, the number remains in your account inventory but will
   * no longer route inbound calls through the previously assigned trunk.
   * URL-encode the phone number (use <code>%2B</code> instead of <code>+</code>).
   */
  public CompletableFuture<Void> unassignNumberFromTrunk(String authId, String phoneNumber,
      RequestOptions requestOptions) {
    return this.rawClient.unassignNumberFromTrunk(authId, phoneNumber, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Remove the assignment between a phone number and a SIP trunk. After
   * unassignment, the number remains in your account inventory but will
   * no longer route inbound calls through the previously assigned trunk.
   * URL-encode the phone number (use <code>%2B</code> instead of <code>+</code>).
   */
  public CompletableFuture<Void> unassignNumberFromTrunk(String authId, String phoneNumber,
      UnassignNumberFromTrunkRequest request) {
    return this.rawClient.unassignNumberFromTrunk(authId, phoneNumber, request).thenApply(response -> response.body());
  }

  /**
   * Remove the assignment between a phone number and a SIP trunk. After
   * unassignment, the number remains in your account inventory but will
   * no longer route inbound calls through the previously assigned trunk.
   * URL-encode the phone number (use <code>%2B</code> instead of <code>+</code>).
   */
  public CompletableFuture<Void> unassignNumberFromTrunk(String authId, String phoneNumber,
      UnassignNumberFromTrunkRequest request, RequestOptions requestOptions) {
    return this.rawClient.unassignNumberFromTrunk(authId, phoneNumber, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the health &amp; analytics dashboard for one of your numbers: current
   * status, spam flag, and call metrics over the selected window (total and
   * answered calls, answer rate, minutes, average duration) plus a per-period
   * time series of snapshots.
   */
  public CompletableFuture<GetNumberHealthResponse> getNumberHealth(String authId, String e164) {
    return this.rawClient.getNumberHealth(authId, e164).thenApply(response -> response.body());
  }

  /**
   * Returns the health &amp; analytics dashboard for one of your numbers: current
   * status, spam flag, and call metrics over the selected window (total and
   * answered calls, answer rate, minutes, average duration) plus a per-period
   * time series of snapshots.
   */
  public CompletableFuture<GetNumberHealthResponse> getNumberHealth(String authId, String e164,
      RequestOptions requestOptions) {
    return this.rawClient.getNumberHealth(authId, e164, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the health &amp; analytics dashboard for one of your numbers: current
   * status, spam flag, and call metrics over the selected window (total and
   * answered calls, answer rate, minutes, average duration) plus a per-period
   * time series of snapshots.
   */
  public CompletableFuture<GetNumberHealthResponse> getNumberHealth(String authId, String e164,
      GetNumberHealthRequest request) {
    return this.rawClient.getNumberHealth(authId, e164, request).thenApply(response -> response.body());
  }

  /**
   * Returns the health &amp; analytics dashboard for one of your numbers: current
   * status, spam flag, and call metrics over the selected window (total and
   * answered calls, answer rate, minutes, average duration) plus a per-period
   * time series of snapshots.
   */
  public CompletableFuture<GetNumberHealthResponse> getNumberHealth(String authId, String e164,
      GetNumberHealthRequest request, RequestOptions requestOptions) {
    return this.rawClient.getNumberHealth(authId, e164, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Assign a parent-pool DID to a sub-account.
   */
  public CompletableFuture<Void> assignDidToSubaccount(String authId, String e164,
      AssignDidToSubaccountRequest request) {
    return this.rawClient.assignDidToSubaccount(authId, e164, request).thenApply(response -> response.body());
  }

  /**
   * Assign a parent-pool DID to a sub-account.
   */
  public CompletableFuture<Void> assignDidToSubaccount(String authId, String e164,
      AssignDidToSubaccountRequest request, RequestOptions requestOptions) {
    return this.rawClient.assignDidToSubaccount(authId, e164, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Move the DID back to the parent pool.
   * <p>A <strong>15-day cool-off</strong> is enforced: if the DID had a call within the last
   * 15 days, the request is rejected with <code>409</code> and a
   * <code>did_cool_off_in_effect</code> error that includes <code>cool_off_until</code> and
   * <code>cool_off_remaining_seconds</code>. Never-used DIDs (<code>last_call_at</code> is <code>NULL</code>)
   * move back immediately.</p>
   * <p>Admins can bypass the cool-off with <code>?force=true</code> (see below); the
   * bypass writes a <code>did_assignment_audit</code> row and requires an
   * admin-role account.</p>
   */
  public CompletableFuture<Void> unassignDidFromSubaccount(String authId, String e164) {
    return this.rawClient.unassignDidFromSubaccount(authId, e164).thenApply(response -> response.body());
  }

  /**
   * Move the DID back to the parent pool.
   * <p>A <strong>15-day cool-off</strong> is enforced: if the DID had a call within the last
   * 15 days, the request is rejected with <code>409</code> and a
   * <code>did_cool_off_in_effect</code> error that includes <code>cool_off_until</code> and
   * <code>cool_off_remaining_seconds</code>. Never-used DIDs (<code>last_call_at</code> is <code>NULL</code>)
   * move back immediately.</p>
   * <p>Admins can bypass the cool-off with <code>?force=true</code> (see below); the
   * bypass writes a <code>did_assignment_audit</code> row and requires an
   * admin-role account.</p>
   */
  public CompletableFuture<Void> unassignDidFromSubaccount(String authId, String e164,
      RequestOptions requestOptions) {
    return this.rawClient.unassignDidFromSubaccount(authId, e164, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Move the DID back to the parent pool.
   * <p>A <strong>15-day cool-off</strong> is enforced: if the DID had a call within the last
   * 15 days, the request is rejected with <code>409</code> and a
   * <code>did_cool_off_in_effect</code> error that includes <code>cool_off_until</code> and
   * <code>cool_off_remaining_seconds</code>. Never-used DIDs (<code>last_call_at</code> is <code>NULL</code>)
   * move back immediately.</p>
   * <p>Admins can bypass the cool-off with <code>?force=true</code> (see below); the
   * bypass writes a <code>did_assignment_audit</code> row and requires an
   * admin-role account.</p>
   */
  public CompletableFuture<Void> unassignDidFromSubaccount(String authId, String e164,
      UnassignDidFromSubaccountRequest request) {
    return this.rawClient.unassignDidFromSubaccount(authId, e164, request).thenApply(response -> response.body());
  }

  /**
   * Move the DID back to the parent pool.
   * <p>A <strong>15-day cool-off</strong> is enforced: if the DID had a call within the last
   * 15 days, the request is rejected with <code>409</code> and a
   * <code>did_cool_off_in_effect</code> error that includes <code>cool_off_until</code> and
   * <code>cool_off_remaining_seconds</code>. Never-used DIDs (<code>last_call_at</code> is <code>NULL</code>)
   * move back immediately.</p>
   * <p>Admins can bypass the cool-off with <code>?force=true</code> (see below); the
   * bypass writes a <code>did_assignment_audit</code> row and requires an
   * admin-role account.</p>
   */
  public CompletableFuture<Void> unassignDidFromSubaccount(String authId, String e164,
      UnassignDidFromSubaccountRequest request, RequestOptions requestOptions) {
    return this.rawClient.unassignDidFromSubaccount(authId, e164, request, requestOptions).thenApply(response -> response.body());
  }
}
