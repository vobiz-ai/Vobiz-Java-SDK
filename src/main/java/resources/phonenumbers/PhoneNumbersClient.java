/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.phonenumbers;

import core.ClientOptions;
import core.RequestOptions;
import java.lang.Object;
import java.lang.String;
import resources.phonenumbers.requests.AssignDidToSubaccountRequest;
import resources.phonenumbers.requests.AssignNumberToTrunkRequest;
import resources.phonenumbers.requests.ListInventoryNumbersRequest;
import resources.phonenumbers.requests.ListNumbersRequest;
import resources.phonenumbers.requests.PurchaseFromInventoryRequest;
import resources.phonenumbers.requests.UnassignDidFromSubaccountRequest;
import resources.phonenumbers.requests.UnassignNumberFromTrunkRequest;
import resources.phonenumbers.requests.UnrentNumberRequest;
import resources.phonenumbers.types.ListInventoryNumbersResponse;
import resources.phonenumbers.types.ListNumbersResponse;

public class PhoneNumbersClient {
  protected final ClientOptions clientOptions;

  private final RawPhoneNumbersClient rawClient;

  public PhoneNumbersClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawPhoneNumbersClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawPhoneNumbersClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * List all phone numbers on your account.
   */
  public ListNumbersResponse listNumbers(String authId) {
    return this.rawClient.listNumbers(authId).body();
  }

  /**
   * List all phone numbers on your account.
   */
  public ListNumbersResponse listNumbers(String authId, RequestOptions requestOptions) {
    return this.rawClient.listNumbers(authId, requestOptions).body();
  }

  /**
   * List all phone numbers on your account.
   */
  public ListNumbersResponse listNumbers(String authId, ListNumbersRequest request) {
    return this.rawClient.listNumbers(authId, request).body();
  }

  /**
   * List all phone numbers on your account.
   */
  public ListNumbersResponse listNumbers(String authId, ListNumbersRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.listNumbers(authId, request, requestOptions).body();
  }

  /**
   * Release a phone number from your account.
   */
  public void unrentNumber(String authId, String e164) {
    this.rawClient.unrentNumber(authId, e164).body();
  }

  /**
   * Release a phone number from your account.
   */
  public void unrentNumber(String authId, String e164, RequestOptions requestOptions) {
    this.rawClient.unrentNumber(authId, e164, requestOptions).body();
  }

  /**
   * Release a phone number from your account.
   */
  public void unrentNumber(String authId, String e164, UnrentNumberRequest request) {
    this.rawClient.unrentNumber(authId, e164, request).body();
  }

  /**
   * Release a phone number from your account.
   */
  public void unrentNumber(String authId, String e164, UnrentNumberRequest request,
      RequestOptions requestOptions) {
    this.rawClient.unrentNumber(authId, e164, request, requestOptions).body();
  }

  /**
   * Browse available phone numbers in inventory that are not assigned to
   * any account. Only numbers with <code>status='active'</code> and <code>auth_id=NULL</code>
   * are returned. These numbers are ready to be purchased.
   */
  public ListInventoryNumbersResponse listInventoryNumbers(String authId) {
    return this.rawClient.listInventoryNumbers(authId).body();
  }

  /**
   * Browse available phone numbers in inventory that are not assigned to
   * any account. Only numbers with <code>status='active'</code> and <code>auth_id=NULL</code>
   * are returned. These numbers are ready to be purchased.
   */
  public ListInventoryNumbersResponse listInventoryNumbers(String authId,
      RequestOptions requestOptions) {
    return this.rawClient.listInventoryNumbers(authId, requestOptions).body();
  }

  /**
   * Browse available phone numbers in inventory that are not assigned to
   * any account. Only numbers with <code>status='active'</code> and <code>auth_id=NULL</code>
   * are returned. These numbers are ready to be purchased.
   */
  public ListInventoryNumbersResponse listInventoryNumbers(String authId,
      ListInventoryNumbersRequest request) {
    return this.rawClient.listInventoryNumbers(authId, request).body();
  }

  /**
   * Browse available phone numbers in inventory that are not assigned to
   * any account. Only numbers with <code>status='active'</code> and <code>auth_id=NULL</code>
   * are returned. These numbers are ready to be purchased.
   */
  public ListInventoryNumbersResponse listInventoryNumbers(String authId,
      ListInventoryNumbersRequest request, RequestOptions requestOptions) {
    return this.rawClient.listInventoryNumbers(authId, request, requestOptions).body();
  }

  /**
   * Purchase a phone number from inventory and assign it to your account.
   * Debits your account balance for the setup fee and monthly fee. For
   * sub-accounts (SA_), the parent master account (MA_) is charged.
   */
  public Object purchaseFromInventory(String authId, PurchaseFromInventoryRequest request) {
    return this.rawClient.purchaseFromInventory(authId, request).body();
  }

  /**
   * Purchase a phone number from inventory and assign it to your account.
   * Debits your account balance for the setup fee and monthly fee. For
   * sub-accounts (SA_), the parent master account (MA_) is charged.
   */
  public Object purchaseFromInventory(String authId, PurchaseFromInventoryRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.purchaseFromInventory(authId, request, requestOptions).body();
  }

  /**
   * Assign a phone number to a specific SIP trunk. Once assigned, all
   * inbound calls to that phone number will be routed through the
   * designated trunk. The phone number must be URL-encoded; use <code>%2B</code>
   * instead of <code>+</code> (e.g., <code>%2B912271264217</code>).
   */
  public void assignNumberToTrunk(String authId, String phoneNumber,
      AssignNumberToTrunkRequest request) {
    this.rawClient.assignNumberToTrunk(authId, phoneNumber, request).body();
  }

  /**
   * Assign a phone number to a specific SIP trunk. Once assigned, all
   * inbound calls to that phone number will be routed through the
   * designated trunk. The phone number must be URL-encoded; use <code>%2B</code>
   * instead of <code>+</code> (e.g., <code>%2B912271264217</code>).
   */
  public void assignNumberToTrunk(String authId, String phoneNumber,
      AssignNumberToTrunkRequest request, RequestOptions requestOptions) {
    this.rawClient.assignNumberToTrunk(authId, phoneNumber, request, requestOptions).body();
  }

  /**
   * Remove the assignment between a phone number and a SIP trunk. After
   * unassignment, the number remains in your account inventory but will
   * no longer route inbound calls through the previously assigned trunk.
   * URL-encode the phone number (use <code>%2B</code> instead of <code>+</code>).
   */
  public void unassignNumberFromTrunk(String authId, String phoneNumber) {
    this.rawClient.unassignNumberFromTrunk(authId, phoneNumber).body();
  }

  /**
   * Remove the assignment between a phone number and a SIP trunk. After
   * unassignment, the number remains in your account inventory but will
   * no longer route inbound calls through the previously assigned trunk.
   * URL-encode the phone number (use <code>%2B</code> instead of <code>+</code>).
   */
  public void unassignNumberFromTrunk(String authId, String phoneNumber,
      RequestOptions requestOptions) {
    this.rawClient.unassignNumberFromTrunk(authId, phoneNumber, requestOptions).body();
  }

  /**
   * Remove the assignment between a phone number and a SIP trunk. After
   * unassignment, the number remains in your account inventory but will
   * no longer route inbound calls through the previously assigned trunk.
   * URL-encode the phone number (use <code>%2B</code> instead of <code>+</code>).
   */
  public void unassignNumberFromTrunk(String authId, String phoneNumber,
      UnassignNumberFromTrunkRequest request) {
    this.rawClient.unassignNumberFromTrunk(authId, phoneNumber, request).body();
  }

  /**
   * Remove the assignment between a phone number and a SIP trunk. After
   * unassignment, the number remains in your account inventory but will
   * no longer route inbound calls through the previously assigned trunk.
   * URL-encode the phone number (use <code>%2B</code> instead of <code>+</code>).
   */
  public void unassignNumberFromTrunk(String authId, String phoneNumber,
      UnassignNumberFromTrunkRequest request, RequestOptions requestOptions) {
    this.rawClient.unassignNumberFromTrunk(authId, phoneNumber, request, requestOptions).body();
  }

  /**
   * Assign a parent-pool DID to a sub-account.
   */
  public void assignDidToSubaccount(String authId, String e164,
      AssignDidToSubaccountRequest request) {
    this.rawClient.assignDidToSubaccount(authId, e164, request).body();
  }

  /**
   * Assign a parent-pool DID to a sub-account.
   */
  public void assignDidToSubaccount(String authId, String e164,
      AssignDidToSubaccountRequest request, RequestOptions requestOptions) {
    this.rawClient.assignDidToSubaccount(authId, e164, request, requestOptions).body();
  }

  /**
   * Move the DID back to the parent pool.
   * <p>A <strong>15-day cool-off</strong> is enforced: if the DID had a call within the last
   * 15 days, the request is rejected with <code>409</code> and a
   * <code>did_cool_off_in_effect</code> error that includes <code>cool_off_until</code> and
   * <code>cool_off_remaining_seconds</code>. Never-used DIDs (<code>last_call_at</code> is <code>NULL</code>)
   * move back immediately.</p>
   * <p>Admins can bypass the cool-off with <code>?force=true</code> (see below); the
   * bypass writes a <code>did_assignment_audit</code> row and requires an
   * admin-role account.</p>
   */
  public void unassignDidFromSubaccount(String authId, String e164) {
    this.rawClient.unassignDidFromSubaccount(authId, e164).body();
  }

  /**
   * Move the DID back to the parent pool.
   * <p>A <strong>15-day cool-off</strong> is enforced: if the DID had a call within the last
   * 15 days, the request is rejected with <code>409</code> and a
   * <code>did_cool_off_in_effect</code> error that includes <code>cool_off_until</code> and
   * <code>cool_off_remaining_seconds</code>. Never-used DIDs (<code>last_call_at</code> is <code>NULL</code>)
   * move back immediately.</p>
   * <p>Admins can bypass the cool-off with <code>?force=true</code> (see below); the
   * bypass writes a <code>did_assignment_audit</code> row and requires an
   * admin-role account.</p>
   */
  public void unassignDidFromSubaccount(String authId, String e164, RequestOptions requestOptions) {
    this.rawClient.unassignDidFromSubaccount(authId, e164, requestOptions).body();
  }

  /**
   * Move the DID back to the parent pool.
   * <p>A <strong>15-day cool-off</strong> is enforced: if the DID had a call within the last
   * 15 days, the request is rejected with <code>409</code> and a
   * <code>did_cool_off_in_effect</code> error that includes <code>cool_off_until</code> and
   * <code>cool_off_remaining_seconds</code>. Never-used DIDs (<code>last_call_at</code> is <code>NULL</code>)
   * move back immediately.</p>
   * <p>Admins can bypass the cool-off with <code>?force=true</code> (see below); the
   * bypass writes a <code>did_assignment_audit</code> row and requires an
   * admin-role account.</p>
   */
  public void unassignDidFromSubaccount(String authId, String e164,
      UnassignDidFromSubaccountRequest request) {
    this.rawClient.unassignDidFromSubaccount(authId, e164, request).body();
  }

  /**
   * Move the DID back to the parent pool.
   * <p>A <strong>15-day cool-off</strong> is enforced: if the DID had a call within the last
   * 15 days, the request is rejected with <code>409</code> and a
   * <code>did_cool_off_in_effect</code> error that includes <code>cool_off_until</code> and
   * <code>cool_off_remaining_seconds</code>. Never-used DIDs (<code>last_call_at</code> is <code>NULL</code>)
   * move back immediately.</p>
   * <p>Admins can bypass the cool-off with <code>?force=true</code> (see below); the
   * bypass writes a <code>did_assignment_audit</code> row and requires an
   * admin-role account.</p>
   */
  public void unassignDidFromSubaccount(String authId, String e164,
      UnassignDidFromSubaccountRequest request, RequestOptions requestOptions) {
    this.rawClient.unassignDidFromSubaccount(authId, e164, request, requestOptions).body();
  }
}
