/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.subaccountkyc;

import core.ClientOptions;
import core.RequestOptions;
import java.lang.Object;
import java.lang.String;
import resources.subaccountkyc.requests.ConfirmSubaccountCinRequest;
import resources.subaccountkyc.requests.CreateSubaccountKycSessionRequest;
import resources.subaccountkyc.requests.GetSubaccountKycStatusRequest;
import resources.subaccountkyc.requests.SearchSubaccountCinRequest;
import resources.subaccountkyc.requests.SubaccountDigilockerInitiateRequest;
import resources.subaccountkyc.requests.SubaccountDigilockerVerifyRequest;
import resources.subaccountkyc.requests.VerifySubaccountGstRequest;
import resources.subaccountkyc.requests.VerifySubaccountPanRequest;
import types.KycVerificationResult;
import types.SubAccountKycStatus;

public class SubAccountKycClient {
  protected final ClientOptions clientOptions;

  private final RawSubAccountKycClient rawClient;

  public SubAccountKycClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawSubAccountKycClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawSubAccountKycClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Returns the aggregated KYC state for a <code>customer_use</code> sub-account —
   * which verifications have passed, whether calls are still blocked, and
   * the business type. The caller must be the parent main account that owns
   * the sub-account (or an admin).
   */
  public SubAccountKycStatus getSubaccountKycStatus(String subAuthId) {
    return this.rawClient.getSubaccountKycStatus(subAuthId).body();
  }

  /**
   * Returns the aggregated KYC state for a <code>customer_use</code> sub-account —
   * which verifications have passed, whether calls are still blocked, and
   * the business type. The caller must be the parent main account that owns
   * the sub-account (or an admin).
   */
  public SubAccountKycStatus getSubaccountKycStatus(String subAuthId,
      RequestOptions requestOptions) {
    return this.rawClient.getSubaccountKycStatus(subAuthId, requestOptions).body();
  }

  /**
   * Returns the aggregated KYC state for a <code>customer_use</code> sub-account —
   * which verifications have passed, whether calls are still blocked, and
   * the business type. The caller must be the parent main account that owns
   * the sub-account (or an admin).
   */
  public SubAccountKycStatus getSubaccountKycStatus(String subAuthId,
      GetSubaccountKycStatusRequest request) {
    return this.rawClient.getSubaccountKycStatus(subAuthId, request).body();
  }

  /**
   * Returns the aggregated KYC state for a <code>customer_use</code> sub-account —
   * which verifications have passed, whether calls are still blocked, and
   * the business type. The caller must be the parent main account that owns
   * the sub-account (or an admin).
   */
  public SubAccountKycStatus getSubaccountKycStatus(String subAuthId,
      GetSubaccountKycStatusRequest request, RequestOptions requestOptions) {
    return this.rawClient.getSubaccountKycStatus(subAuthId, request, requestOptions).body();
  }

  /**
   * Runs a real PAN verification (Perfios) for the sub-account. <code>pan</code> must
   * be exactly 10 characters. Persists a <code>kyc_verifications</code> row and
   * recomputes the sub-account's aggregated <code>kyc_status</code>.
   */
  public KycVerificationResult verifySubaccountPan(String subAuthId,
      VerifySubaccountPanRequest request) {
    return this.rawClient.verifySubaccountPan(subAuthId, request).body();
  }

  /**
   * Runs a real PAN verification (Perfios) for the sub-account. <code>pan</code> must
   * be exactly 10 characters. Persists a <code>kyc_verifications</code> row and
   * recomputes the sub-account's aggregated <code>kyc_status</code>.
   */
  public KycVerificationResult verifySubaccountPan(String subAuthId,
      VerifySubaccountPanRequest request, RequestOptions requestOptions) {
    return this.rawClient.verifySubaccountPan(subAuthId, request, requestOptions).body();
  }

  /**
   * Runs a real GSTIN verification. <code>gstin</code> must be a 15-character GSTIN.
   */
  public KycVerificationResult verifySubaccountGst(String subAuthId,
      VerifySubaccountGstRequest request) {
    return this.rawClient.verifySubaccountGst(subAuthId, request).body();
  }

  /**
   * Runs a real GSTIN verification. <code>gstin</code> must be a 15-character GSTIN.
   */
  public KycVerificationResult verifySubaccountGst(String subAuthId,
      VerifySubaccountGstRequest request, RequestOptions requestOptions) {
    return this.rawClient.verifySubaccountGst(subAuthId, request, requestOptions).body();
  }

  /**
   * Name-based CIN lookup. Returns candidate company matches; pick one and
   * pass it to <a href="#operation/confirm-subaccount-cin">CIN confirm</a>.
   */
  public Object searchSubaccountCin(String subAuthId, SearchSubaccountCinRequest request) {
    return this.rawClient.searchSubaccountCin(subAuthId, request).body();
  }

  /**
   * Name-based CIN lookup. Returns candidate company matches; pick one and
   * pass it to <a href="#operation/confirm-subaccount-cin">CIN confirm</a>.
   */
  public Object searchSubaccountCin(String subAuthId, SearchSubaccountCinRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.searchSubaccountCin(subAuthId, request, requestOptions).body();
  }

  /**
   * Confirm the CIN selected from the search results.
   */
  public KycVerificationResult confirmSubaccountCin(String subAuthId,
      ConfirmSubaccountCinRequest request) {
    return this.rawClient.confirmSubaccountCin(subAuthId, request).body();
  }

  /**
   * Confirm the CIN selected from the search results.
   */
  public KycVerificationResult confirmSubaccountCin(String subAuthId,
      ConfirmSubaccountCinRequest request, RequestOptions requestOptions) {
    return this.rawClient.confirmSubaccountCin(subAuthId, request, requestOptions).body();
  }

  /**
   * Returns the DigiLocker authorization link and an <code>access_request_id</code>.
   * The customer completes the OAuth flow on the DigiLocker portal, after
   * which you finalize with
   * <a href="#operation/subaccount-digilocker-verify">DigiLocker verify</a>.
   */
  public Object subaccountDigilockerInitiate(String subAuthId,
      SubaccountDigilockerInitiateRequest request) {
    return this.rawClient.subaccountDigilockerInitiate(subAuthId, request).body();
  }

  /**
   * Returns the DigiLocker authorization link and an <code>access_request_id</code>.
   * The customer completes the OAuth flow on the DigiLocker portal, after
   * which you finalize with
   * <a href="#operation/subaccount-digilocker-verify">DigiLocker verify</a>.
   */
  public Object subaccountDigilockerInitiate(String subAuthId,
      SubaccountDigilockerInitiateRequest request, RequestOptions requestOptions) {
    return this.rawClient.subaccountDigilockerInitiate(subAuthId, request, requestOptions).body();
  }

  /**
   * Finalize Aadhaar via DigiLocker after the customer completes OAuth.
   */
  public KycVerificationResult subaccountDigilockerVerify(String subAuthId,
      SubaccountDigilockerVerifyRequest request) {
    return this.rawClient.subaccountDigilockerVerify(subAuthId, request).body();
  }

  /**
   * Finalize Aadhaar via DigiLocker after the customer completes OAuth.
   */
  public KycVerificationResult subaccountDigilockerVerify(String subAuthId,
      SubaccountDigilockerVerifyRequest request, RequestOptions requestOptions) {
    return this.rawClient.subaccountDigilockerVerify(subAuthId, request, requestOptions).body();
  }

  /**
   * Creates a Vobiz-hosted KYC session for the sub-account. With
   * <code>flow_type=email</code> (default) Vobiz emails the customer a signed link
   * (from <code>kyc@vobiz.ai</code>, hosted at <code>kyc.vobiz.ai</code>) and <code>customer_email</code> is
   * required. With <code>flow_type=redirect</code>, omit <code>customer_email</code>, pass a
   * <code>redirect_url</code>, and the <code>widget_url</code> is returned directly for an inline
   * redirect.
   * <p>This is the sub-account–scoped equivalent of the partner-level
   * <a href="/partner/api/kyc-sessions">KYC Sessions</a> endpoint.</p>
   */
  public Object createSubaccountKycSession(String subAuthId,
      CreateSubaccountKycSessionRequest request) {
    return this.rawClient.createSubaccountKycSession(subAuthId, request).body();
  }

  /**
   * Creates a Vobiz-hosted KYC session for the sub-account. With
   * <code>flow_type=email</code> (default) Vobiz emails the customer a signed link
   * (from <code>kyc@vobiz.ai</code>, hosted at <code>kyc.vobiz.ai</code>) and <code>customer_email</code> is
   * required. With <code>flow_type=redirect</code>, omit <code>customer_email</code>, pass a
   * <code>redirect_url</code>, and the <code>widget_url</code> is returned directly for an inline
   * redirect.
   * <p>This is the sub-account–scoped equivalent of the partner-level
   * <a href="/partner/api/kyc-sessions">KYC Sessions</a> endpoint.</p>
   */
  public Object createSubaccountKycSession(String subAuthId,
      CreateSubaccountKycSessionRequest request, RequestOptions requestOptions) {
    return this.rawClient.createSubaccountKycSession(subAuthId, request, requestOptions).body();
  }
}
