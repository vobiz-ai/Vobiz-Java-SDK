/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.subaccountkyc;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import errors.BadRequestError;
import errors.ForbiddenError;
import errors.NotFoundError;
import errors.UnprocessableEntityError;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.subaccountkyc.requests.ConfirmSubaccountCinRequest;
import resources.subaccountkyc.requests.CreateSubaccountKycSessionRequest;
import resources.subaccountkyc.requests.GetSubaccountKycStatusRequest;
import resources.subaccountkyc.requests.SearchSubaccountCinRequest;
import resources.subaccountkyc.requests.SubaccountDigilockerInitiateRequest;
import resources.subaccountkyc.requests.SubaccountDigilockerVerifyRequest;
import resources.subaccountkyc.requests.VerifySubaccountGstRequest;
import resources.subaccountkyc.requests.VerifySubaccountPanRequest;
import types.Error;
import types.KycVerificationResult;
import types.SubAccountKycStatus;

public class RawSubAccountKycClient {
  protected final ClientOptions clientOptions;

  public RawSubAccountKycClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns the aggregated KYC state for a <code>customer_use</code> sub-account —
   * which verifications have passed, whether calls are still blocked, and
   * the business type. The caller must be the parent main account that owns
   * the sub-account (or an admin).
   */
  public VobizApiHttpResponse<SubAccountKycStatus> getSubaccountKycStatus(String subAuthId) {
    return getSubaccountKycStatus(subAuthId,GetSubaccountKycStatusRequest.builder().build());
  }

  /**
   * Returns the aggregated KYC state for a <code>customer_use</code> sub-account —
   * which verifications have passed, whether calls are still blocked, and
   * the business type. The caller must be the parent main account that owns
   * the sub-account (or an admin).
   */
  public VobizApiHttpResponse<SubAccountKycStatus> getSubaccountKycStatus(String subAuthId,
      RequestOptions requestOptions) {
    return getSubaccountKycStatus(subAuthId,GetSubaccountKycStatusRequest.builder().build(),requestOptions);
  }

  /**
   * Returns the aggregated KYC state for a <code>customer_use</code> sub-account —
   * which verifications have passed, whether calls are still blocked, and
   * the business type. The caller must be the parent main account that owns
   * the sub-account (or an admin).
   */
  public VobizApiHttpResponse<SubAccountKycStatus> getSubaccountKycStatus(String subAuthId,
      GetSubaccountKycStatusRequest request) {
    return getSubaccountKycStatus(subAuthId,request,null);
  }

  /**
   * Returns the aggregated KYC state for a <code>customer_use</code> sub-account —
   * which verifications have passed, whether calls are still blocked, and
   * the business type. The caller must be the parent main account that owns
   * the sub-account (or an admin).
   */
  public VobizApiHttpResponse<SubAccountKycStatus> getSubaccountKycStatus(String subAuthId,
      GetSubaccountKycStatusRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/sub-accounts")
      .addPathSegment(subAuthId)
      .addPathSegments("kyc")
      .addPathSegments("status");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubAccountKycStatus.class), response);
        }
        try {
          switch (response.code()) {
            case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Runs a real PAN verification (Perfios) for the sub-account. <code>pan</code> must
     * be exactly 10 characters. Persists a <code>kyc_verifications</code> row and
     * recomputes the sub-account's aggregated <code>kyc_status</code>.
     */
    public VobizApiHttpResponse<KycVerificationResult> verifySubaccountPan(String subAuthId,
        VerifySubaccountPanRequest request) {
      return verifySubaccountPan(subAuthId,request,null);
    }

    /**
     * Runs a real PAN verification (Perfios) for the sub-account. <code>pan</code> must
     * be exactly 10 characters. Persists a <code>kyc_verifications</code> row and
     * recomputes the sub-account's aggregated <code>kyc_status</code>.
     */
    public VobizApiHttpResponse<KycVerificationResult> verifySubaccountPan(String subAuthId,
        VerifySubaccountPanRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/sub-accounts")
        .addPathSegment(subAuthId)
        .addPathSegments("kyc")
        .addPathSegments("verify-pan");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new VobizApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, KycVerificationResult.class), response);
          }
          try {
            if (response.code() == 400) {
              throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Runs a real GSTIN verification. <code>gstin</code> must be a 15-character GSTIN.
       */
      public VobizApiHttpResponse<KycVerificationResult> verifySubaccountGst(String subAuthId,
          VerifySubaccountGstRequest request) {
        return verifySubaccountGst(subAuthId,request,null);
      }

      /**
       * Runs a real GSTIN verification. <code>gstin</code> must be a 15-character GSTIN.
       */
      public VobizApiHttpResponse<KycVerificationResult> verifySubaccountGst(String subAuthId,
          VerifySubaccountGstRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/sub-accounts")
          .addPathSegment(subAuthId)
          .addPathSegments("kyc")
          .addPathSegments("verify-gst");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new VobizApiException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, KycVerificationResult.class), response);
            }
            try {
              if (response.code() == 400) {
                throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VobizApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Name-based CIN lookup. Returns candidate company matches; pick one and
         * pass it to <a href="#operation/confirm-subaccount-cin">CIN confirm</a>.
         */
        public VobizApiHttpResponse<Object> searchSubaccountCin(String subAuthId,
            SearchSubaccountCinRequest request) {
          return searchSubaccountCin(subAuthId,request,null);
        }

        /**
         * Name-based CIN lookup. Returns candidate company matches; pick one and
         * pass it to <a href="#operation/confirm-subaccount-cin">CIN confirm</a>.
         */
        public VobizApiHttpResponse<Object> searchSubaccountCin(String subAuthId,
            SearchSubaccountCinRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/sub-accounts")
            .addPathSegment(subAuthId)
            .addPathSegments("kyc/cin")
            .addPathSegments("search");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VobizApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VobizApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Confirm the CIN selected from the search results.
           */
          public VobizApiHttpResponse<KycVerificationResult> confirmSubaccountCin(String subAuthId,
              ConfirmSubaccountCinRequest request) {
            return confirmSubaccountCin(subAuthId,request,null);
          }

          /**
           * Confirm the CIN selected from the search results.
           */
          public VobizApiHttpResponse<KycVerificationResult> confirmSubaccountCin(String subAuthId,
              ConfirmSubaccountCinRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/v1/sub-accounts")
              .addPathSegment(subAuthId)
              .addPathSegments("kyc/cin")
              .addPathSegments("confirm");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
              }
              catch(JsonProcessingException e) {
                throw new VobizApiException("Failed to serialize request", e);
              }
              Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, KycVerificationResult.class), response);
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new VobizApiException("Network error executing HTTP request", e);
              }
            }

            /**
             * Returns the DigiLocker authorization link and an <code>access_request_id</code>.
             * The customer completes the OAuth flow on the DigiLocker portal, after
             * which you finalize with
             * <a href="#operation/subaccount-digilocker-verify">DigiLocker verify</a>.
             */
            public VobizApiHttpResponse<Object> subaccountDigilockerInitiate(String subAuthId,
                SubaccountDigilockerInitiateRequest request) {
              return subaccountDigilockerInitiate(subAuthId,request,null);
            }

            /**
             * Returns the DigiLocker authorization link and an <code>access_request_id</code>.
             * The customer completes the OAuth flow on the DigiLocker portal, after
             * which you finalize with
             * <a href="#operation/subaccount-digilocker-verify">DigiLocker verify</a>.
             */
            public VobizApiHttpResponse<Object> subaccountDigilockerInitiate(String subAuthId,
                SubaccountDigilockerInitiateRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("api/v1/sub-accounts")
                .addPathSegment(subAuthId)
                .addPathSegments("kyc/digilocker")
                .addPathSegments("initiate");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                }
                catch(JsonProcessingException e) {
                  throw new VobizApiException("Failed to serialize request", e);
                }
                Request okhttpRequest = new Request.Builder()
                  .url(httpUrl.build())
                  .method("POST", body)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/json")
                  .addHeader("Accept", "application/json")
                  .build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new VobizApiException("Network error executing HTTP request", e);
                }
              }

              /**
               * Finalize Aadhaar via DigiLocker after the customer completes OAuth.
               */
              public VobizApiHttpResponse<KycVerificationResult> subaccountDigilockerVerify(
                  String subAuthId, SubaccountDigilockerVerifyRequest request) {
                return subaccountDigilockerVerify(subAuthId,request,null);
              }

              /**
               * Finalize Aadhaar via DigiLocker after the customer completes OAuth.
               */
              public VobizApiHttpResponse<KycVerificationResult> subaccountDigilockerVerify(
                  String subAuthId, SubaccountDigilockerVerifyRequest request,
                  RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("api/v1/sub-accounts")
                  .addPathSegment(subAuthId)
                  .addPathSegments("kyc/digilocker")
                  .addPathSegments("verify");if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  RequestBody body;
                  try {
                    body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                  }
                  catch(JsonProcessingException e) {
                    throw new VobizApiException("Failed to serialize request", e);
                  }
                  Request okhttpRequest = new Request.Builder()
                    .url(httpUrl.build())
                    .method("POST", body)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Content-Type", "application/json")
                    .addHeader("Accept", "application/json")
                    .build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, KycVerificationResult.class), response);
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (IOException e) {
                    throw new VobizApiException("Network error executing HTTP request", e);
                  }
                }

                /**
                 * Creates a Vobiz-hosted KYC session for the sub-account. With
                 * <code>flow_type=email</code> (default) Vobiz emails the customer a signed link
                 * (from <code>kyc@vobiz.ai</code>, hosted at <code>kyc.vobiz.ai</code>) and <code>customer_email</code> is
                 * required. With <code>flow_type=redirect</code>, omit <code>customer_email</code>, pass a
                 * <code>redirect_url</code>, and the <code>widget_url</code> is returned directly for an inline
                 * redirect.
                 * <p>This is the sub-account–scoped equivalent of the partner-level
                 * <a href="/partner/api/kyc-sessions">KYC Sessions</a> endpoint.</p>
                 */
                public VobizApiHttpResponse<Object> createSubaccountKycSession(String subAuthId,
                    CreateSubaccountKycSessionRequest request) {
                  return createSubaccountKycSession(subAuthId,request,null);
                }

                /**
                 * Creates a Vobiz-hosted KYC session for the sub-account. With
                 * <code>flow_type=email</code> (default) Vobiz emails the customer a signed link
                 * (from <code>kyc@vobiz.ai</code>, hosted at <code>kyc.vobiz.ai</code>) and <code>customer_email</code> is
                 * required. With <code>flow_type=redirect</code>, omit <code>customer_email</code>, pass a
                 * <code>redirect_url</code>, and the <code>widget_url</code> is returned directly for an inline
                 * redirect.
                 * <p>This is the sub-account–scoped equivalent of the partner-level
                 * <a href="/partner/api/kyc-sessions">KYC Sessions</a> endpoint.</p>
                 */
                public VobizApiHttpResponse<Object> createSubaccountKycSession(String subAuthId,
                    CreateSubaccountKycSessionRequest request, RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                    .addPathSegments("api/v1/sub-accounts")
                    .addPathSegment(subAuthId)
                    .addPathSegments("kyc-sessions");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    RequestBody body;
                    try {
                      body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                    }
                    catch(JsonProcessingException e) {
                      throw new VobizApiException("Failed to serialize request", e);
                    }
                    Request okhttpRequest = new Request.Builder()
                      .url(httpUrl.build())
                      .method("POST", body)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Content-Type", "application/json")
                      .addHeader("Accept", "application/json")
                      .build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    try (Response response = client.newCall(okhttpRequest).execute()) {
                      ResponseBody responseBody = response.body();
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      }
                      try {
                        if (response.code() == 422) {
                          throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                    }
                    catch (IOException e) {
                      throw new VobizApiException("Network error executing HTTP request", e);
                    }
                  }
                }
