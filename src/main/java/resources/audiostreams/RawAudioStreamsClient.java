/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.audiostreams;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.lang.Void;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.audiostreams.requests.GetStreamRequest;
import resources.audiostreams.requests.ListStreamsRequest;
import resources.audiostreams.requests.StartStreamRequest;
import resources.audiostreams.requests.StopStreamRequest;

public class RawAudioStreamsClient {
  protected final ClientOptions clientOptions;

  public RawAudioStreamsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * List all audio streams on a live call.
   */
  public VobizApiHttpResponse<Void> listStreams(String authId, String callUuid) {
    return listStreams(authId,callUuid,ListStreamsRequest.builder().build());
  }

  /**
   * List all audio streams on a live call.
   */
  public VobizApiHttpResponse<Void> listStreams(String authId, String callUuid,
      RequestOptions requestOptions) {
    return listStreams(authId,callUuid,ListStreamsRequest.builder().build(),requestOptions);
  }

  /**
   * List all audio streams on a live call.
   */
  public VobizApiHttpResponse<Void> listStreams(String authId, String callUuid,
      ListStreamsRequest request) {
    return listStreams(authId,callUuid,request,null);
  }

  /**
   * List all audio streams on a live call.
   */
  public VobizApiHttpResponse<Void> listStreams(String authId, String callUuid,
      ListStreamsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("Call")
      .addPathSegment(callUuid)
      .addPathSegments("Stream");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)));
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(null, response);
        }
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Start streaming raw audio from a live call to a WebSocket URL.
     */
    public VobizApiHttpResponse<Object> startStream(String authId, String callUuid,
        StartStreamRequest request) {
      return startStream(authId,callUuid,request,null);
    }

    /**
     * Start streaming raw audio from a live call to a WebSocket URL.
     */
    public VobizApiHttpResponse<Object> startStream(String authId, String callUuid,
        StartStreamRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("Call")
        .addPathSegment(callUuid)
        .addPathSegments("Stream");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new VobizApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Get details of a specific audio stream.
       */
      public VobizApiHttpResponse<Void> getStream(String authId, String callUuid, String streamId) {
        return getStream(authId,callUuid,streamId,GetStreamRequest.builder().build());
      }

      /**
       * Get details of a specific audio stream.
       */
      public VobizApiHttpResponse<Void> getStream(String authId, String callUuid, String streamId,
          RequestOptions requestOptions) {
        return getStream(authId,callUuid,streamId,GetStreamRequest.builder().build(),requestOptions);
      }

      /**
       * Get details of a specific audio stream.
       */
      public VobizApiHttpResponse<Void> getStream(String authId, String callUuid, String streamId,
          GetStreamRequest request) {
        return getStream(authId,callUuid,streamId,request,null);
      }

      /**
       * Get details of a specific audio stream.
       */
      public VobizApiHttpResponse<Void> getStream(String authId, String callUuid, String streamId,
          GetStreamRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/Account")
          .addPathSegment(authId)
          .addPathSegments("Call")
          .addPathSegment(callUuid)
          .addPathSegments("Stream")
          .addPathSegment(streamId);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)));
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
              return new VobizApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VobizApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Stop a specific audio stream on a live call.
         */
        public VobizApiHttpResponse<Void> stopStream(String authId, String callUuid,
            String streamId) {
          return stopStream(authId,callUuid,streamId,StopStreamRequest.builder().build());
        }

        /**
         * Stop a specific audio stream on a live call.
         */
        public VobizApiHttpResponse<Void> stopStream(String authId, String callUuid,
            String streamId, RequestOptions requestOptions) {
          return stopStream(authId,callUuid,streamId,StopStreamRequest.builder().build(),requestOptions);
        }

        /**
         * Stop a specific audio stream on a live call.
         */
        public VobizApiHttpResponse<Void> stopStream(String authId, String callUuid,
            String streamId, StopStreamRequest request) {
          return stopStream(authId,callUuid,streamId,request,null);
        }

        /**
         * Stop a specific audio stream on a live call.
         */
        public VobizApiHttpResponse<Void> stopStream(String authId, String callUuid,
            String streamId, StopStreamRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/Account")
            .addPathSegment(authId)
            .addPathSegments("Call")
            .addPathSegment(callUuid)
            .addPathSegments("Stream")
            .addPathSegment(streamId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("DELETE", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)));
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              if (response.isSuccessful()) {
                return new VobizApiHttpResponse<>(null, response);
              }
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VobizApiException("Network error executing HTTP request", e);
            }
          }
        }
