/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.calls;

import core.ClientOptions;
import core.RequestOptions;
import java.lang.String;
import resources.calls.requests.MakeCallRequest;
import resources.calls.types.MakeCallResponse;

public class CallsClient {
  protected final ClientOptions clientOptions;

  private final RawCallsClient rawClient;

  public CallsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawCallsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawCallsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Initiate an outbound call to a PSTN number or SIP endpoint.
   * Use <code>&lt;</code> to separate multiple destinations (max 1000).
   */
  public MakeCallResponse makeCall(String authId, MakeCallRequest request) {
    return this.rawClient.makeCall(authId, request).body();
  }

  /**
   * Initiate an outbound call to a PSTN number or SIP endpoint.
   * Use <code>&lt;</code> to separate multiple destinations (max 1000).
   */
  public MakeCallResponse makeCall(String authId, MakeCallRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.makeCall(authId, request, requestOptions).body();
  }
}
