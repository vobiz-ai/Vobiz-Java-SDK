/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.endpoints;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import errors.BadRequestError;
import errors.NotFoundError;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;
import resources.endpoints.requests.CreateEndpointRequest;
import resources.endpoints.requests.DeleteEndpointRequest;
import resources.endpoints.requests.ListEndpointsRequest;
import resources.endpoints.requests.RetrieveEndpointRequest;
import resources.endpoints.requests.UpdateEndpointRequest;
import resources.endpoints.types.CreateEndpointResponse;
import resources.endpoints.types.ListEndpointsResponse;
import resources.endpoints.types.RetrieveEndpointResponse;

public class AsyncRawEndpointsClient {
  protected final ClientOptions clientOptions;

  public AsyncRawEndpointsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve a paginated list of all SIP endpoints in your account.
   */
  public CompletableFuture<VobizApiHttpResponse<ListEndpointsResponse>> listEndpoints(
      String authId) {
    return listEndpoints(authId,ListEndpointsRequest.builder().build());
  }

  /**
   * Retrieve a paginated list of all SIP endpoints in your account.
   */
  public CompletableFuture<VobizApiHttpResponse<ListEndpointsResponse>> listEndpoints(String authId,
      RequestOptions requestOptions) {
    return listEndpoints(authId,ListEndpointsRequest.builder().build(),requestOptions);
  }

  /**
   * Retrieve a paginated list of all SIP endpoints in your account.
   */
  public CompletableFuture<VobizApiHttpResponse<ListEndpointsResponse>> listEndpoints(String authId,
      ListEndpointsRequest request) {
    return listEndpoints(authId,request,null);
  }

  /**
   * Retrieve a paginated list of all SIP endpoints in your account.
   */
  public CompletableFuture<VobizApiHttpResponse<ListEndpointsResponse>> listEndpoints(String authId,
      ListEndpointsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("Endpoint");if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (request.getOffset().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
      }
      if (request.getUsernameContains().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "username__contains", request.getUsernameContains().get(), false);
      }
      if (request.getUsernameExact().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "username__exact", request.getUsernameExact().get(), false);
      }
      if (request.getUsernameStartswith().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "username__startswith", request.getUsernameStartswith().get(), false);
      }
      if (request.getAliasContains().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "alias__contains", request.getAliasContains().get(), false);
      }
      if (request.getAliasExact().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "alias__exact", request.getAliasExact().get(), false);
      }
      if (request.getApplicationIdExact().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "application_id__exact", request.getApplicationIdExact().get(), false);
      }
      if (request.getApplicationIdIsnull().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "application_id__isnull", request.getApplicationIdIsnull().get(), false);
      }
      if (request.getSubAccount().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sub_account", request.getSubAccount().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      CompletableFuture<VobizApiHttpResponse<ListEndpointsResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListEndpointsResponse.class), response));
              return;
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (IOException e) {
            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Create a new SIP endpoint that can be used to make and receive calls
     * through IP phones, softphones, or SIP clients. Each endpoint is
     * assigned a unique SIP URI and endpoint ID.
     */
    public CompletableFuture<VobizApiHttpResponse<CreateEndpointResponse>> createEndpoint(
        String authId, CreateEndpointRequest request) {
      return createEndpoint(authId,request,null);
    }

    /**
     * Create a new SIP endpoint that can be used to make and receive calls
     * through IP phones, softphones, or SIP clients. Each endpoint is
     * assigned a unique SIP URI and endpoint ID.
     */
    public CompletableFuture<VobizApiHttpResponse<CreateEndpointResponse>> createEndpoint(
        String authId, CreateEndpointRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("Endpoint");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new VobizApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<VobizApiHttpResponse<CreateEndpointResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateEndpointResponse.class), response));
                return;
              }
              try {
                if (response.code() == 400) {
                  future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (IOException e) {
              future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Retrieve the details of an existing endpoint. The response includes
       * all endpoint attributes and, if the endpoint is currently registered
       * on a SIP client, additional registration details.
       */
      public CompletableFuture<VobizApiHttpResponse<RetrieveEndpointResponse>> retrieveEndpoint(
          String authId, String endpointId) {
        return retrieveEndpoint(authId,endpointId,RetrieveEndpointRequest.builder().build());
      }

      /**
       * Retrieve the details of an existing endpoint. The response includes
       * all endpoint attributes and, if the endpoint is currently registered
       * on a SIP client, additional registration details.
       */
      public CompletableFuture<VobizApiHttpResponse<RetrieveEndpointResponse>> retrieveEndpoint(
          String authId, String endpointId, RequestOptions requestOptions) {
        return retrieveEndpoint(authId,endpointId,RetrieveEndpointRequest.builder().build(),requestOptions);
      }

      /**
       * Retrieve the details of an existing endpoint. The response includes
       * all endpoint attributes and, if the endpoint is currently registered
       * on a SIP client, additional registration details.
       */
      public CompletableFuture<VobizApiHttpResponse<RetrieveEndpointResponse>> retrieveEndpoint(
          String authId, String endpointId, RetrieveEndpointRequest request) {
        return retrieveEndpoint(authId,endpointId,request,null);
      }

      /**
       * Retrieve the details of an existing endpoint. The response includes
       * all endpoint attributes and, if the endpoint is currently registered
       * on a SIP client, additional registration details.
       */
      public CompletableFuture<VobizApiHttpResponse<RetrieveEndpointResponse>> retrieveEndpoint(
          String authId, String endpointId, RetrieveEndpointRequest request,
          RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/Account")
          .addPathSegment(authId)
          .addPathSegments("Endpoint")
          .addPathSegment(endpointId);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          CompletableFuture<VobizApiHttpResponse<RetrieveEndpointResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrieveEndpointResponse.class), response));
                  return;
                }
                try {
                  if (response.code() == 404) {
                    future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (IOException e) {
                future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Update an existing endpoint's configuration. You can change the
         * password, alias, or attached application. The fields <code>username</code>,
         * <code>endpoint_id</code>, <code>domain</code>, <code>allow_same_domain</code>, <code>allow_other_domains</code>,
         * <code>allow_phones</code>, and <code>allow_apps</code> are locked after creation.
         */
        public CompletableFuture<VobizApiHttpResponse<String>> updateEndpoint(String authId,
            String endpointId, UpdateEndpointRequest request) {
          return updateEndpoint(authId,endpointId,request,null);
        }

        /**
         * Update an existing endpoint's configuration. You can change the
         * password, alias, or attached application. The fields <code>username</code>,
         * <code>endpoint_id</code>, <code>domain</code>, <code>allow_same_domain</code>, <code>allow_other_domains</code>,
         * <code>allow_phones</code>, and <code>allow_apps</code> are locked after creation.
         */
        public CompletableFuture<VobizApiHttpResponse<String>> updateEndpoint(String authId,
            String endpointId, UpdateEndpointRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/Account")
            .addPathSegment(authId)
            .addPathSegments("Endpoint")
            .addPathSegment(endpointId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VobizApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            CompletableFuture<VobizApiHttpResponse<String>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, String.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (IOException e) {
                  future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Permanently delete an endpoint from your Vobiz account. Once deleted,
           * the SIP URI will no longer be accessible, and any devices registered
           * with this endpoint will be disconnected.
           */
          public CompletableFuture<VobizApiHttpResponse<Optional<String>>> deleteEndpoint(
              String authId, String endpointId) {
            return deleteEndpoint(authId,endpointId,DeleteEndpointRequest.builder().build());
          }

          /**
           * Permanently delete an endpoint from your Vobiz account. Once deleted,
           * the SIP URI will no longer be accessible, and any devices registered
           * with this endpoint will be disconnected.
           */
          public CompletableFuture<VobizApiHttpResponse<Optional<String>>> deleteEndpoint(
              String authId, String endpointId, RequestOptions requestOptions) {
            return deleteEndpoint(authId,endpointId,DeleteEndpointRequest.builder().build(),requestOptions);
          }

          /**
           * Permanently delete an endpoint from your Vobiz account. Once deleted,
           * the SIP URI will no longer be accessible, and any devices registered
           * with this endpoint will be disconnected.
           */
          public CompletableFuture<VobizApiHttpResponse<Optional<String>>> deleteEndpoint(
              String authId, String endpointId, DeleteEndpointRequest request) {
            return deleteEndpoint(authId,endpointId,request,null);
          }

          /**
           * Permanently delete an endpoint from your Vobiz account. Once deleted,
           * the SIP URI will no longer be accessible, and any devices registered
           * with this endpoint will be disconnected.
           */
          public CompletableFuture<VobizApiHttpResponse<Optional<String>>> deleteEndpoint(
              String authId, String endpointId, DeleteEndpointRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/v1/Account")
              .addPathSegment(authId)
              .addPathSegments("Endpoint")
              .addPathSegment(endpointId);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              CompletableFuture<VobizApiHttpResponse<Optional<String>>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<Optional<String>>() {}), response));
                      return;
                    }
                    try {
                      if (response.code() == 404) {
                        future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                }
              });
              return future;
            }
          }
