/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.conferencemembers;

import core.ClientOptions;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.lang.Void;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.conferencemembers.requests.MuteMemberRequest;
import resources.conferencemembers.requests.UnmuteMemberRequest;

public class RawConferenceMembersClient {
  protected final ClientOptions clientOptions;

  public RawConferenceMembersClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Prevent a member from speaking. Use <code>all</code> as member_id to mute everyone.
   */
  public VobizApiHttpResponse<Object> muteMember(String authId, String conferenceName,
      String memberId) {
    return muteMember(authId,conferenceName,memberId,MuteMemberRequest.builder().build());
  }

  /**
   * Prevent a member from speaking. Use <code>all</code> as member_id to mute everyone.
   */
  public VobizApiHttpResponse<Object> muteMember(String authId, String conferenceName,
      String memberId, RequestOptions requestOptions) {
    return muteMember(authId,conferenceName,memberId,MuteMemberRequest.builder().build(),requestOptions);
  }

  /**
   * Prevent a member from speaking. Use <code>all</code> as member_id to mute everyone.
   */
  public VobizApiHttpResponse<Object> muteMember(String authId, String conferenceName,
      String memberId, MuteMemberRequest request) {
    return muteMember(authId,conferenceName,memberId,request,null);
  }

  /**
   * Prevent a member from speaking. Use <code>all</code> as member_id to mute everyone.
   */
  public VobizApiHttpResponse<Object> muteMember(String authId, String conferenceName,
      String memberId, MuteMemberRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("Conference")
      .addPathSegment(conferenceName)
      .addPathSegments("Member")
      .addPathSegment(memberId)
      .addPathSegments("Mute");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("POST", RequestBody.create("", null))
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Allow a muted member to speak again.
     */
    public VobizApiHttpResponse<Void> unmuteMember(String authId, String conferenceName,
        String memberId) {
      return unmuteMember(authId,conferenceName,memberId,UnmuteMemberRequest.builder().build());
    }

    /**
     * Allow a muted member to speak again.
     */
    public VobizApiHttpResponse<Void> unmuteMember(String authId, String conferenceName,
        String memberId, RequestOptions requestOptions) {
      return unmuteMember(authId,conferenceName,memberId,UnmuteMemberRequest.builder().build(),requestOptions);
    }

    /**
     * Allow a muted member to speak again.
     */
    public VobizApiHttpResponse<Void> unmuteMember(String authId, String conferenceName,
        String memberId, UnmuteMemberRequest request) {
      return unmuteMember(authId,conferenceName,memberId,request,null);
    }

    /**
     * Allow a muted member to speak again.
     */
    public VobizApiHttpResponse<Void> unmuteMember(String authId, String conferenceName,
        String memberId, UnmuteMemberRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("Conference")
        .addPathSegment(conferenceName)
        .addPathSegments("Member")
        .addPathSegment(memberId)
        .addPathSegments("Mute");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("DELETE", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(null, response);
          }
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }
    }
