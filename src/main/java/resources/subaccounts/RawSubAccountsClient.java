/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.subaccounts;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.subaccounts.requests.CreateSubaccountRequest;
import resources.subaccounts.requests.DeleteSubaccountRequest;
import resources.subaccounts.requests.ListSubaccountsRequest;
import resources.subaccounts.requests.RetrieveSubaccountRequest;
import resources.subaccounts.requests.UpdateSubaccountRequest;
import resources.subaccounts.types.CreateSubaccountResponse;
import resources.subaccounts.types.DeleteSubaccountResponse;
import resources.subaccounts.types.ListSubaccountsResponse;
import resources.subaccounts.types.RetrieveSubaccountResponse;
import resources.subaccounts.types.UpdateSubaccountResponse;

public class RawSubAccountsClient {
  protected final ClientOptions clientOptions;

  public RawSubAccountsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve all sub-accounts under the master account.
   */
  public VobizApiHttpResponse<ListSubaccountsResponse> listSubaccounts(String authId) {
    return listSubaccounts(authId,ListSubaccountsRequest.builder().build());
  }

  /**
   * Retrieve all sub-accounts under the master account.
   */
  public VobizApiHttpResponse<ListSubaccountsResponse> listSubaccounts(String authId,
      RequestOptions requestOptions) {
    return listSubaccounts(authId,ListSubaccountsRequest.builder().build(),requestOptions);
  }

  /**
   * Retrieve all sub-accounts under the master account.
   */
  public VobizApiHttpResponse<ListSubaccountsResponse> listSubaccounts(String authId,
      ListSubaccountsRequest request) {
    return listSubaccounts(authId,request,null);
  }

  /**
   * Retrieve all sub-accounts under the master account.
   */
  public VobizApiHttpResponse<ListSubaccountsResponse> listSubaccounts(String authId,
      ListSubaccountsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/accounts")
      .addPathSegment(authId)
      .addPathSegments("sub-accounts");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListSubaccountsResponse.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Create a new sub-account under the master account.
     * <p>Set <code>kyc_mode</code> to control how the sub-account is verified:</p>
     * <ul>
     * <li><code>personal_use</code> <em>(default)</em> — the sub-account inherits the parent's
     * KYC; no separate verification is required.</li>
     * <li><code>customer_use</code> — the sub-account must complete its own KYC before it
     * can place calls. A fresh <code>customer_use</code> sub-account is returned with
     * <code>kyc_calls_blocked: true</code>. <code>customer_use</code> <strong>requires</strong> <code>email</code>.</li>
     * </ul>
     */
    public VobizApiHttpResponse<CreateSubaccountResponse> createSubaccount(String authId,
        CreateSubaccountRequest request) {
      return createSubaccount(authId,request,null);
    }

    /**
     * Create a new sub-account under the master account.
     * <p>Set <code>kyc_mode</code> to control how the sub-account is verified:</p>
     * <ul>
     * <li><code>personal_use</code> <em>(default)</em> — the sub-account inherits the parent's
     * KYC; no separate verification is required.</li>
     * <li><code>customer_use</code> — the sub-account must complete its own KYC before it
     * can place calls. A fresh <code>customer_use</code> sub-account is returned with
     * <code>kyc_calls_blocked: true</code>. <code>customer_use</code> <strong>requires</strong> <code>email</code>.</li>
     * </ul>
     */
    public VobizApiHttpResponse<CreateSubaccountResponse> createSubaccount(String authId,
        CreateSubaccountRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/accounts")
        .addPathSegment(authId)
        .addPathSegments("sub-accounts");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new VobizApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateSubaccountResponse.class), response);
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Retrieve details of a specific sub-account.
       */
      public VobizApiHttpResponse<RetrieveSubaccountResponse> retrieveSubaccount(String authId,
          String subAuthId) {
        return retrieveSubaccount(authId,subAuthId,RetrieveSubaccountRequest.builder().build());
      }

      /**
       * Retrieve details of a specific sub-account.
       */
      public VobizApiHttpResponse<RetrieveSubaccountResponse> retrieveSubaccount(String authId,
          String subAuthId, RequestOptions requestOptions) {
        return retrieveSubaccount(authId,subAuthId,RetrieveSubaccountRequest.builder().build(),requestOptions);
      }

      /**
       * Retrieve details of a specific sub-account.
       */
      public VobizApiHttpResponse<RetrieveSubaccountResponse> retrieveSubaccount(String authId,
          String subAuthId, RetrieveSubaccountRequest request) {
        return retrieveSubaccount(authId,subAuthId,request,null);
      }

      /**
       * Retrieve details of a specific sub-account.
       */
      public VobizApiHttpResponse<RetrieveSubaccountResponse> retrieveSubaccount(String authId,
          String subAuthId, RetrieveSubaccountRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/accounts")
          .addPathSegment(authId)
          .addPathSegments("sub-accounts")
          .addPathSegment(subAuthId);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrieveSubaccountResponse.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VobizApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Update the name or status of a sub-account, or change its <code>kyc_mode</code>.
         * <p>Promoting an existing sub-account to <code>customer_use</code> requires the
         * sub-account to already have an <code>email</code> (otherwise <code>400</code>). On any
         * <code>kyc_mode</code> change, <code>kyc_calls_blocked</code> is re-derived from the
         * sub-account's current KYC state.</p>
         */
        public VobizApiHttpResponse<UpdateSubaccountResponse> updateSubaccount(String authId,
            String subAuthId) {
          return updateSubaccount(authId,subAuthId,UpdateSubaccountRequest.builder().build());
        }

        /**
         * Update the name or status of a sub-account, or change its <code>kyc_mode</code>.
         * <p>Promoting an existing sub-account to <code>customer_use</code> requires the
         * sub-account to already have an <code>email</code> (otherwise <code>400</code>). On any
         * <code>kyc_mode</code> change, <code>kyc_calls_blocked</code> is re-derived from the
         * sub-account's current KYC state.</p>
         */
        public VobizApiHttpResponse<UpdateSubaccountResponse> updateSubaccount(String authId,
            String subAuthId, RequestOptions requestOptions) {
          return updateSubaccount(authId,subAuthId,UpdateSubaccountRequest.builder().build(),requestOptions);
        }

        /**
         * Update the name or status of a sub-account, or change its <code>kyc_mode</code>.
         * <p>Promoting an existing sub-account to <code>customer_use</code> requires the
         * sub-account to already have an <code>email</code> (otherwise <code>400</code>). On any
         * <code>kyc_mode</code> change, <code>kyc_calls_blocked</code> is re-derived from the
         * sub-account's current KYC state.</p>
         */
        public VobizApiHttpResponse<UpdateSubaccountResponse> updateSubaccount(String authId,
            String subAuthId, UpdateSubaccountRequest request) {
          return updateSubaccount(authId,subAuthId,request,null);
        }

        /**
         * Update the name or status of a sub-account, or change its <code>kyc_mode</code>.
         * <p>Promoting an existing sub-account to <code>customer_use</code> requires the
         * sub-account to already have an <code>email</code> (otherwise <code>400</code>). On any
         * <code>kyc_mode</code> change, <code>kyc_calls_blocked</code> is re-derived from the
         * sub-account's current KYC state.</p>
         */
        public VobizApiHttpResponse<UpdateSubaccountResponse> updateSubaccount(String authId,
            String subAuthId, UpdateSubaccountRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/accounts")
            .addPathSegment(authId)
            .addPathSegments("sub-accounts")
            .addPathSegment(subAuthId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VobizApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("PUT", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateSubaccountResponse.class), response);
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VobizApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Permanently delete a sub-account and revoke its credentials.
           */
          public VobizApiHttpResponse<Optional<DeleteSubaccountResponse>> deleteSubaccount(
              String authId, String subAuthId) {
            return deleteSubaccount(authId,subAuthId,DeleteSubaccountRequest.builder().build());
          }

          /**
           * Permanently delete a sub-account and revoke its credentials.
           */
          public VobizApiHttpResponse<Optional<DeleteSubaccountResponse>> deleteSubaccount(
              String authId, String subAuthId, RequestOptions requestOptions) {
            return deleteSubaccount(authId,subAuthId,DeleteSubaccountRequest.builder().build(),requestOptions);
          }

          /**
           * Permanently delete a sub-account and revoke its credentials.
           */
          public VobizApiHttpResponse<Optional<DeleteSubaccountResponse>> deleteSubaccount(
              String authId, String subAuthId, DeleteSubaccountRequest request) {
            return deleteSubaccount(authId,subAuthId,request,null);
          }

          /**
           * Permanently delete a sub-account and revoke its credentials.
           */
          public VobizApiHttpResponse<Optional<DeleteSubaccountResponse>> deleteSubaccount(
              String authId, String subAuthId, DeleteSubaccountRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/v1/accounts")
              .addPathSegment(authId)
              .addPathSegments("sub-accounts")
              .addPathSegment(subAuthId);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<Optional<DeleteSubaccountResponse>>() {}), response);
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new VobizApiException("Network error executing HTTP request", e);
              }
            }
          }
