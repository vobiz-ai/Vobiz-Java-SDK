/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.applications;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import errors.BadRequestError;
import errors.ConflictError;
import errors.NotFoundError;
import errors.UnauthorizedError;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.applications.requests.CreateApplicationRequest;
import resources.applications.requests.DeleteApplicationRequest;
import resources.applications.requests.ListApplicationsRequest;
import resources.applications.requests.RetrieveApplicationRequest;
import resources.applications.requests.UpdateApplicationRequest;
import resources.applications.types.CreateApplicationResponse;
import resources.applications.types.ListApplicationsResponse;
import resources.applications.types.RetrieveApplicationResponse;
import resources.applications.types.UpdateApplicationResponse;

public class RawApplicationsClient {
  protected final ClientOptions clientOptions;

  public RawApplicationsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Get details of all applications created under your Vobiz account.
   */
  public VobizApiHttpResponse<ListApplicationsResponse> listApplications(String authId) {
    return listApplications(authId,ListApplicationsRequest.builder().build());
  }

  /**
   * Get details of all applications created under your Vobiz account.
   */
  public VobizApiHttpResponse<ListApplicationsResponse> listApplications(String authId,
      RequestOptions requestOptions) {
    return listApplications(authId,ListApplicationsRequest.builder().build(),requestOptions);
  }

  /**
   * Get details of all applications created under your Vobiz account.
   */
  public VobizApiHttpResponse<ListApplicationsResponse> listApplications(String authId,
      ListApplicationsRequest request) {
    return listApplications(authId,request,null);
  }

  /**
   * Get details of all applications created under your Vobiz account.
   */
  public VobizApiHttpResponse<ListApplicationsResponse> listApplications(String authId,
      ListApplicationsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("Application");if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (request.getOffset().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListApplicationsResponse.class), response);
        }
        try {
          if (response.code() == 401) {
            throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Creates an Application with webhook URLs for call handling.
     * Creating an application is usually a first step, after which you
     * attach the application to either a number or an endpoint.
     */
    public VobizApiHttpResponse<CreateApplicationResponse> createApplication(String authId,
        CreateApplicationRequest request) {
      return createApplication(authId,request,null);
    }

    /**
     * Creates an Application with webhook URLs for call handling.
     * Creating an application is usually a first step, after which you
     * attach the application to either a number or an endpoint.
     */
    public VobizApiHttpResponse<CreateApplicationResponse> createApplication(String authId,
        CreateApplicationRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("Application");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new VobizApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateApplicationResponse.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Get details of a particular application by passing the app_id.
       */
      public VobizApiHttpResponse<RetrieveApplicationResponse> retrieveApplication(String authId,
          String appId) {
        return retrieveApplication(authId,appId,RetrieveApplicationRequest.builder().build());
      }

      /**
       * Get details of a particular application by passing the app_id.
       */
      public VobizApiHttpResponse<RetrieveApplicationResponse> retrieveApplication(String authId,
          String appId, RequestOptions requestOptions) {
        return retrieveApplication(authId,appId,RetrieveApplicationRequest.builder().build(),requestOptions);
      }

      /**
       * Get details of a particular application by passing the app_id.
       */
      public VobizApiHttpResponse<RetrieveApplicationResponse> retrieveApplication(String authId,
          String appId, RetrieveApplicationRequest request) {
        return retrieveApplication(authId,appId,request,null);
      }

      /**
       * Get details of a particular application by passing the app_id.
       */
      public VobizApiHttpResponse<RetrieveApplicationResponse> retrieveApplication(String authId,
          String appId, RetrieveApplicationRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/Account")
          .addPathSegment(authId)
          .addPathSegments("Application")
          .addPathSegment(appId);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrieveApplicationResponse.class), response);
            }
            try {
              if (response.code() == 404) {
                throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VobizApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Modify an application using this API. You can update any subset of
         * fields (partial update). Fields not provided will remain unchanged.
         */
        public VobizApiHttpResponse<UpdateApplicationResponse> updateApplication(String authId,
            String appId, UpdateApplicationRequest request) {
          return updateApplication(authId,appId,request,null);
        }

        /**
         * Modify an application using this API. You can update any subset of
         * fields (partial update). Fields not provided will remain unchanged.
         */
        public VobizApiHttpResponse<UpdateApplicationResponse> updateApplication(String authId,
            String appId, UpdateApplicationRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/Account")
            .addPathSegment(authId)
            .addPathSegments("Application")
            .addPathSegment(appId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VobizApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateApplicationResponse.class), response);
              }
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VobizApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Permanently delete an Application. If the application is associated
           * with phone numbers, the deletion may be blocked unless those
           * associations are removed first.
           */
          public VobizApiHttpResponse<Optional<String>> deleteApplication(String authId,
              String appId) {
            return deleteApplication(authId,appId,DeleteApplicationRequest.builder().build());
          }

          /**
           * Permanently delete an Application. If the application is associated
           * with phone numbers, the deletion may be blocked unless those
           * associations are removed first.
           */
          public VobizApiHttpResponse<Optional<String>> deleteApplication(String authId,
              String appId, RequestOptions requestOptions) {
            return deleteApplication(authId,appId,DeleteApplicationRequest.builder().build(),requestOptions);
          }

          /**
           * Permanently delete an Application. If the application is associated
           * with phone numbers, the deletion may be blocked unless those
           * associations are removed first.
           */
          public VobizApiHttpResponse<Optional<String>> deleteApplication(String authId,
              String appId, DeleteApplicationRequest request) {
            return deleteApplication(authId,appId,request,null);
          }

          /**
           * Permanently delete an Application. If the application is associated
           * with phone numbers, the deletion may be blocked unless those
           * associations are removed first.
           */
          public VobizApiHttpResponse<Optional<String>> deleteApplication(String authId,
              String appId, DeleteApplicationRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/v1/Account")
              .addPathSegment(authId)
              .addPathSegments("Application")
              .addPathSegment(appId);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<Optional<String>>() {}), response);
                }
                try {
                  switch (response.code()) {
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 409:throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new VobizApiException("Network error executing HTTP request", e);
              }
            }
          }
