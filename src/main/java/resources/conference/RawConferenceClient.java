/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.conference;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.lang.Void;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.conference.requests.DeafMemberRequest;
import resources.conference.requests.HangupMemberRequest;
import resources.conference.requests.KickMemberRequest;
import resources.conference.requests.PlayAudioMemberRequest;
import resources.conference.requests.StopAudioMemberRequest;
import resources.conference.requests.UndeafMemberRequest;

public class RawConferenceClient {
  protected final ClientOptions clientOptions;

  public RawConferenceClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Remove a specific participant from a conference call.
   */
  public VobizApiHttpResponse<Object> kickMember(String authId, String conferenceName,
      String memberId) {
    return kickMember(authId,conferenceName,memberId,KickMemberRequest.builder().build());
  }

  /**
   * Remove a specific participant from a conference call.
   */
  public VobizApiHttpResponse<Object> kickMember(String authId, String conferenceName,
      String memberId, RequestOptions requestOptions) {
    return kickMember(authId,conferenceName,memberId,KickMemberRequest.builder().build(),requestOptions);
  }

  /**
   * Remove a specific participant from a conference call.
   */
  public VobizApiHttpResponse<Object> kickMember(String authId, String conferenceName,
      String memberId, KickMemberRequest request) {
    return kickMember(authId,conferenceName,memberId,request,null);
  }

  /**
   * Remove a specific participant from a conference call.
   */
  public VobizApiHttpResponse<Object> kickMember(String authId, String conferenceName,
      String memberId, KickMemberRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("Conference")
      .addPathSegment(conferenceName)
      .addPathSegments("Member")
      .addPathSegment(memberId)
      .addPathSegments("Kick");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("POST", RequestBody.create("", null))
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Disconnect a specific member from a conference.
     */
    public VobizApiHttpResponse<Void> hangupMember(String authId, String conferenceName,
        String memberId) {
      return hangupMember(authId,conferenceName,memberId,HangupMemberRequest.builder().build());
    }

    /**
     * Disconnect a specific member from a conference.
     */
    public VobizApiHttpResponse<Void> hangupMember(String authId, String conferenceName,
        String memberId, RequestOptions requestOptions) {
      return hangupMember(authId,conferenceName,memberId,HangupMemberRequest.builder().build(),requestOptions);
    }

    /**
     * Disconnect a specific member from a conference.
     */
    public VobizApiHttpResponse<Void> hangupMember(String authId, String conferenceName,
        String memberId, HangupMemberRequest request) {
      return hangupMember(authId,conferenceName,memberId,request,null);
    }

    /**
     * Disconnect a specific member from a conference.
     */
    public VobizApiHttpResponse<Void> hangupMember(String authId, String conferenceName,
        String memberId, HangupMemberRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("Conference")
        .addPathSegment(conferenceName)
        .addPathSegments("Member")
        .addPathSegment(memberId);if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("DELETE", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(null, response);
          }
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Play an audio file to a specific conference member.
       */
      public VobizApiHttpResponse<Void> playAudioMember(String authId, String conferenceName,
          String memberId, PlayAudioMemberRequest request) {
        return playAudioMember(authId,conferenceName,memberId,request,null);
      }

      /**
       * Play an audio file to a specific conference member.
       */
      public VobizApiHttpResponse<Void> playAudioMember(String authId, String conferenceName,
          String memberId, PlayAudioMemberRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/Account")
          .addPathSegment(authId)
          .addPathSegments("Conference")
          .addPathSegment(conferenceName)
          .addPathSegments("Member")
          .addPathSegment(memberId)
          .addPathSegments("Play");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new VobizApiException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
              return new VobizApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VobizApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Stop audio playback for a specific conference member.
         */
        public VobizApiHttpResponse<Void> stopAudioMember(String authId, String conferenceName,
            String memberId) {
          return stopAudioMember(authId,conferenceName,memberId,StopAudioMemberRequest.builder().build());
        }

        /**
         * Stop audio playback for a specific conference member.
         */
        public VobizApiHttpResponse<Void> stopAudioMember(String authId, String conferenceName,
            String memberId, RequestOptions requestOptions) {
          return stopAudioMember(authId,conferenceName,memberId,StopAudioMemberRequest.builder().build(),requestOptions);
        }

        /**
         * Stop audio playback for a specific conference member.
         */
        public VobizApiHttpResponse<Void> stopAudioMember(String authId, String conferenceName,
            String memberId, StopAudioMemberRequest request) {
          return stopAudioMember(authId,conferenceName,memberId,request,null);
        }

        /**
         * Stop audio playback for a specific conference member.
         */
        public VobizApiHttpResponse<Void> stopAudioMember(String authId, String conferenceName,
            String memberId, StopAudioMemberRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/Account")
            .addPathSegment(authId)
            .addPathSegments("Conference")
            .addPathSegment(conferenceName)
            .addPathSegments("Member")
            .addPathSegment(memberId)
            .addPathSegments("Play");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("DELETE", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)));
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              if (response.isSuccessful()) {
                return new VobizApiHttpResponse<>(null, response);
              }
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VobizApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Prevent a conference member from hearing other participants.
           */
          public VobizApiHttpResponse<Void> deafMember(String authId, String conferenceName,
              String memberId) {
            return deafMember(authId,conferenceName,memberId,DeafMemberRequest.builder().build());
          }

          /**
           * Prevent a conference member from hearing other participants.
           */
          public VobizApiHttpResponse<Void> deafMember(String authId, String conferenceName,
              String memberId, RequestOptions requestOptions) {
            return deafMember(authId,conferenceName,memberId,DeafMemberRequest.builder().build(),requestOptions);
          }

          /**
           * Prevent a conference member from hearing other participants.
           */
          public VobizApiHttpResponse<Void> deafMember(String authId, String conferenceName,
              String memberId, DeafMemberRequest request) {
            return deafMember(authId,conferenceName,memberId,request,null);
          }

          /**
           * Prevent a conference member from hearing other participants.
           */
          public VobizApiHttpResponse<Void> deafMember(String authId, String conferenceName,
              String memberId, DeafMemberRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/v1/Account")
              .addPathSegment(authId)
              .addPathSegments("Conference")
              .addPathSegment(conferenceName)
              .addPathSegments("Member")
              .addPathSegment(memberId)
              .addPathSegments("Deaf");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)));
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                if (response.isSuccessful()) {
                  return new VobizApiHttpResponse<>(null, response);
                }
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new VobizApiException("Network error executing HTTP request", e);
              }
            }

            /**
             * Restore a conference member's ability to hear other participants.
             */
            public VobizApiHttpResponse<Void> undeafMember(String authId, String conferenceName,
                String memberId) {
              return undeafMember(authId,conferenceName,memberId,UndeafMemberRequest.builder().build());
            }

            /**
             * Restore a conference member's ability to hear other participants.
             */
            public VobizApiHttpResponse<Void> undeafMember(String authId, String conferenceName,
                String memberId, RequestOptions requestOptions) {
              return undeafMember(authId,conferenceName,memberId,UndeafMemberRequest.builder().build(),requestOptions);
            }

            /**
             * Restore a conference member's ability to hear other participants.
             */
            public VobizApiHttpResponse<Void> undeafMember(String authId, String conferenceName,
                String memberId, UndeafMemberRequest request) {
              return undeafMember(authId,conferenceName,memberId,request,null);
            }

            /**
             * Restore a conference member's ability to hear other participants.
             */
            public VobizApiHttpResponse<Void> undeafMember(String authId, String conferenceName,
                String memberId, UndeafMemberRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("api/v1/Account")
                .addPathSegment(authId)
                .addPathSegments("Conference")
                .addPathSegment(conferenceName)
                .addPathSegments("Member")
                .addPathSegment(memberId)
                .addPathSegments("Deaf");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("DELETE", null)
                  .headers(Headers.of(clientOptions.headers(requestOptions)));
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  if (response.isSuccessful()) {
                    return new VobizApiHttpResponse<>(null, response);
                  }
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new VobizApiException("Network error executing HTTP request", e);
                }
              }
            }
