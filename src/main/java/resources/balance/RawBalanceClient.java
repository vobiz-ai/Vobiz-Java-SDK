/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.balance;

import core.ClientOptions;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.balance.requests.GetBalanceRequest;
import resources.balance.requests.ListTransactionsRequest;
import resources.balance.types.GetBalanceResponse;
import resources.balance.types.ListTransactionsResponse;

public class RawBalanceClient {
  protected final ClientOptions clientOptions;

  public RawBalanceClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve the current account balance for a specific currency.
   */
  public VobizApiHttpResponse<GetBalanceResponse> getBalance(String authId, String currency) {
    return getBalance(authId,currency,GetBalanceRequest.builder().build());
  }

  /**
   * Retrieve the current account balance for a specific currency.
   */
  public VobizApiHttpResponse<GetBalanceResponse> getBalance(String authId, String currency,
      RequestOptions requestOptions) {
    return getBalance(authId,currency,GetBalanceRequest.builder().build(),requestOptions);
  }

  /**
   * Retrieve the current account balance for a specific currency.
   */
  public VobizApiHttpResponse<GetBalanceResponse> getBalance(String authId, String currency,
      GetBalanceRequest request) {
    return getBalance(authId,currency,request,null);
  }

  /**
   * Retrieve the current account balance for a specific currency.
   */
  public VobizApiHttpResponse<GetBalanceResponse> getBalance(String authId, String currency,
      GetBalanceRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("balance")
      .addPathSegment(currency);if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetBalanceResponse.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Retrieve paginated transaction history for the account.
     */
    public VobizApiHttpResponse<ListTransactionsResponse> listTransactions(String authId) {
      return listTransactions(authId,ListTransactionsRequest.builder().build());
    }

    /**
     * Retrieve paginated transaction history for the account.
     */
    public VobizApiHttpResponse<ListTransactionsResponse> listTransactions(String authId,
        RequestOptions requestOptions) {
      return listTransactions(authId,ListTransactionsRequest.builder().build(),requestOptions);
    }

    /**
     * Retrieve paginated transaction history for the account.
     */
    public VobizApiHttpResponse<ListTransactionsResponse> listTransactions(String authId,
        ListTransactionsRequest request) {
      return listTransactions(authId,request,null);
    }

    /**
     * Retrieve paginated transaction history for the account.
     */
    public VobizApiHttpResponse<ListTransactionsResponse> listTransactions(String authId,
        ListTransactionsRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("transactions");if (request.getLimit().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getOffset().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
        }
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListTransactionsResponse.class), response);
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }
    }
