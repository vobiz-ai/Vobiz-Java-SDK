/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.balance;

import core.ClientOptions;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;
import resources.balance.requests.GetBalanceRequest;
import resources.balance.requests.ListTransactionsRequest;
import resources.balance.types.GetBalanceResponse;
import resources.balance.types.ListTransactionsResponse;

public class AsyncRawBalanceClient {
  protected final ClientOptions clientOptions;

  public AsyncRawBalanceClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve the current account balance for a specific currency.
   */
  public CompletableFuture<VobizApiHttpResponse<GetBalanceResponse>> getBalance(String authId,
      String currency) {
    return getBalance(authId,currency,GetBalanceRequest.builder().build());
  }

  /**
   * Retrieve the current account balance for a specific currency.
   */
  public CompletableFuture<VobizApiHttpResponse<GetBalanceResponse>> getBalance(String authId,
      String currency, RequestOptions requestOptions) {
    return getBalance(authId,currency,GetBalanceRequest.builder().build(),requestOptions);
  }

  /**
   * Retrieve the current account balance for a specific currency.
   */
  public CompletableFuture<VobizApiHttpResponse<GetBalanceResponse>> getBalance(String authId,
      String currency, GetBalanceRequest request) {
    return getBalance(authId,currency,request,null);
  }

  /**
   * Retrieve the current account balance for a specific currency.
   */
  public CompletableFuture<VobizApiHttpResponse<GetBalanceResponse>> getBalance(String authId,
      String currency, GetBalanceRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("balance")
      .addPathSegment(currency);if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      CompletableFuture<VobizApiHttpResponse<GetBalanceResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetBalanceResponse.class), response));
              return;
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (IOException e) {
            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Retrieve paginated transaction history for the account.
     */
    public CompletableFuture<VobizApiHttpResponse<ListTransactionsResponse>> listTransactions(
        String authId) {
      return listTransactions(authId,ListTransactionsRequest.builder().build());
    }

    /**
     * Retrieve paginated transaction history for the account.
     */
    public CompletableFuture<VobizApiHttpResponse<ListTransactionsResponse>> listTransactions(
        String authId, RequestOptions requestOptions) {
      return listTransactions(authId,ListTransactionsRequest.builder().build(),requestOptions);
    }

    /**
     * Retrieve paginated transaction history for the account.
     */
    public CompletableFuture<VobizApiHttpResponse<ListTransactionsResponse>> listTransactions(
        String authId, ListTransactionsRequest request) {
      return listTransactions(authId,request,null);
    }

    /**
     * Retrieve paginated transaction history for the account.
     */
    public CompletableFuture<VobizApiHttpResponse<ListTransactionsResponse>> listTransactions(
        String authId, ListTransactionsRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("transactions");if (request.getLimit().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getOffset().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
        }
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<VobizApiHttpResponse<ListTransactionsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListTransactionsResponse.class), response));
                return;
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (IOException e) {
              future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
          }
        });
        return future;
      }
    }
