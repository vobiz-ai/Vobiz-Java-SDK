/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.subaccountkyctestmode;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.subaccountkyctestmode.requests.MockConfirmSubaccountCinRequest;
import resources.subaccountkyctestmode.requests.MockFinalizePendingKycRequest;
import resources.subaccountkyctestmode.requests.MockSearchSubaccountCinRequest;
import resources.subaccountkyctestmode.requests.MockSubaccountDigilockerInitiateRequest;
import resources.subaccountkyctestmode.requests.MockSubaccountDigilockerVerifyRequest;
import resources.subaccountkyctestmode.requests.MockVerifySubaccountGstRequest;
import resources.subaccountkyctestmode.requests.MockVerifySubaccountPanRequest;
import types.KycVerificationResult;

public class RawSubAccountKycTestModeClient {
  protected final ClientOptions clientOptions;

  public RawSubAccountKycTestModeClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Mock PAN verification - never hits the provider. Magic <code>pan</code> inputs:
   * <p>| Input | Outcome |
   * |---|---|
   * | <code>TESTSUCCESS0001</code> | verified |
   * | <code>TESTFAIL0001</code> | failed |
   * | <code>TESTERROR0001</code> | HTTP 500 |
   * | <code>TESTPENDING001</code> | pending (finalize as verified) |
   * | <code>TESTPENDING_FAIL</code> | pending (finalize as failed) |</p>
   * <p>Persists a real <code>kyc_verifications</code> row and recomputes <code>kyc_status</code>.</p>
   */
  public VobizApiHttpResponse<KycVerificationResult> mockVerifySubaccountPan(String subAuthId,
      MockVerifySubaccountPanRequest request) {
    return mockVerifySubaccountPan(subAuthId,request,null);
  }

  /**
   * Mock PAN verification - never hits the provider. Magic <code>pan</code> inputs:
   * <p>| Input | Outcome |
   * |---|---|
   * | <code>TESTSUCCESS0001</code> | verified |
   * | <code>TESTFAIL0001</code> | failed |
   * | <code>TESTERROR0001</code> | HTTP 500 |
   * | <code>TESTPENDING001</code> | pending (finalize as verified) |
   * | <code>TESTPENDING_FAIL</code> | pending (finalize as failed) |</p>
   * <p>Persists a real <code>kyc_verifications</code> row and recomputes <code>kyc_status</code>.</p>
   */
  public VobizApiHttpResponse<KycVerificationResult> mockVerifySubaccountPan(String subAuthId,
      MockVerifySubaccountPanRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/sub-accounts/test")
      .addPathSegment(subAuthId)
      .addPathSegments("kyc")
      .addPathSegments("verify-pan");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      RequestBody body;
      try {
        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
      }
      catch(JsonProcessingException e) {
        throw new VobizApiException("Failed to serialize request", e);
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("POST", body)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Content-Type", "application/json")
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, KycVerificationResult.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Mock GST verification. Same magic-input matrix as <a href="#operation/mock-verify-subaccount-pan">Mock verify PAN</a>.
     */
    public VobizApiHttpResponse<KycVerificationResult> mockVerifySubaccountGst(String subAuthId,
        MockVerifySubaccountGstRequest request) {
      return mockVerifySubaccountGst(subAuthId,request,null);
    }

    /**
     * Mock GST verification. Same magic-input matrix as <a href="#operation/mock-verify-subaccount-pan">Mock verify PAN</a>.
     */
    public VobizApiHttpResponse<KycVerificationResult> mockVerifySubaccountGst(String subAuthId,
        MockVerifySubaccountGstRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/sub-accounts/test")
        .addPathSegment(subAuthId)
        .addPathSegments("kyc")
        .addPathSegments("verify-gst");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new VobizApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, KycVerificationResult.class), response);
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Returns deterministic fake company matches.
       */
      public VobizApiHttpResponse<Object> mockSearchSubaccountCin(String subAuthId,
          MockSearchSubaccountCinRequest request) {
        return mockSearchSubaccountCin(subAuthId,request,null);
      }

      /**
       * Returns deterministic fake company matches.
       */
      public VobizApiHttpResponse<Object> mockSearchSubaccountCin(String subAuthId,
          MockSearchSubaccountCinRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/sub-accounts/test")
          .addPathSegment(subAuthId)
          .addPathSegments("kyc/cin")
          .addPathSegments("search");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new VobizApiException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VobizApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Succeeds when <code>selected_cin</code> starts with <code>U72900KA2024PTC123456</code>.
         */
        public VobizApiHttpResponse<KycVerificationResult> mockConfirmSubaccountCin(
            String subAuthId, MockConfirmSubaccountCinRequest request) {
          return mockConfirmSubaccountCin(subAuthId,request,null);
        }

        /**
         * Succeeds when <code>selected_cin</code> starts with <code>U72900KA2024PTC123456</code>.
         */
        public VobizApiHttpResponse<KycVerificationResult> mockConfirmSubaccountCin(
            String subAuthId, MockConfirmSubaccountCinRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/sub-accounts/test")
            .addPathSegment(subAuthId)
            .addPathSegments("kyc/cin")
            .addPathSegments("confirm");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VobizApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, KycVerificationResult.class), response);
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VobizApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Returns a deterministic <code>access_request_id</code>.
           */
          public VobizApiHttpResponse<Object> mockSubaccountDigilockerInitiate(String subAuthId,
              MockSubaccountDigilockerInitiateRequest request) {
            return mockSubaccountDigilockerInitiate(subAuthId,request,null);
          }

          /**
           * Returns a deterministic <code>access_request_id</code>.
           */
          public VobizApiHttpResponse<Object> mockSubaccountDigilockerInitiate(String subAuthId,
              MockSubaccountDigilockerInitiateRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/v1/sub-accounts/test")
              .addPathSegment(subAuthId)
              .addPathSegments("kyc/digilocker")
              .addPathSegments("initiate");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
              }
              catch(JsonProcessingException e) {
                throw new VobizApiException("Failed to serialize request", e);
              }
              Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new VobizApiException("Network error executing HTTP request", e);
              }
            }

            /**
             * <code>access_request_id</code> <code>MOCK_AR_SUCCESS</code> → verified; <code>MOCK_AR_FAIL</code> → failed.
             */
            public VobizApiHttpResponse<KycVerificationResult> mockSubaccountDigilockerVerify(
                String subAuthId, MockSubaccountDigilockerVerifyRequest request) {
              return mockSubaccountDigilockerVerify(subAuthId,request,null);
            }

            /**
             * <code>access_request_id</code> <code>MOCK_AR_SUCCESS</code> → verified; <code>MOCK_AR_FAIL</code> → failed.
             */
            public VobizApiHttpResponse<KycVerificationResult> mockSubaccountDigilockerVerify(
                String subAuthId, MockSubaccountDigilockerVerifyRequest request,
                RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("api/v1/sub-accounts/test")
                .addPathSegment(subAuthId)
                .addPathSegments("kyc/digilocker")
                .addPathSegments("verify");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                }
                catch(JsonProcessingException e) {
                  throw new VobizApiException("Failed to serialize request", e);
                }
                Request okhttpRequest = new Request.Builder()
                  .url(httpUrl.build())
                  .method("POST", body)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/json")
                  .addHeader("Accept", "application/json")
                  .build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, KycVerificationResult.class), response);
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new VobizApiException("Network error executing HTTP request", e);
                }
              }

              /**
               * Promotes the most recent <strong>pending</strong> mock verification of the given
               * type to a terminal outcome - this drives the async (<code>TESTPENDING…</code>)
               * path without webhooks. <code>verification_type</code> ∈ <code>pan | aadhaar | gst | cin</code>;
               * <code>outcome</code> ∈ <code>verified | failed</code>.
               */
              public VobizApiHttpResponse<KycVerificationResult> mockFinalizePendingKyc(
                  String subAuthId, MockFinalizePendingKycRequest request) {
                return mockFinalizePendingKyc(subAuthId,request,null);
              }

              /**
               * Promotes the most recent <strong>pending</strong> mock verification of the given
               * type to a terminal outcome - this drives the async (<code>TESTPENDING…</code>)
               * path without webhooks. <code>verification_type</code> ∈ <code>pan | aadhaar | gst | cin</code>;
               * <code>outcome</code> ∈ <code>verified | failed</code>.
               */
              public VobizApiHttpResponse<KycVerificationResult> mockFinalizePendingKyc(
                  String subAuthId, MockFinalizePendingKycRequest request,
                  RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("api/v1/sub-accounts/test")
                  .addPathSegment(subAuthId)
                  .addPathSegments("kyc")
                  .addPathSegments("finalize-pending");if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  RequestBody body;
                  try {
                    body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                  }
                  catch(JsonProcessingException e) {
                    throw new VobizApiException("Failed to serialize request", e);
                  }
                  Request okhttpRequest = new Request.Builder()
                    .url(httpUrl.build())
                    .method("POST", body)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Content-Type", "application/json")
                    .addHeader("Accept", "application/json")
                    .build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, KycVerificationResult.class), response);
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (IOException e) {
                    throw new VobizApiException("Network error executing HTTP request", e);
                  }
                }
              }
