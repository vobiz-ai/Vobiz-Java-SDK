/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.partnerapi;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import errors.BadRequestError;
import errors.ConflictError;
import errors.NotFoundError;
import errors.TooManyRequestsError;
import errors.UnprocessableEntityError;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.Void;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;
import resources.partnerapi.requests.CreateCustomerAccountRequest;
import resources.partnerapi.requests.CreateKycSessionRequest;
import resources.partnerapi.requests.GetKycSessionRequest;
import resources.partnerapi.requests.ListCustomerAccountsRequest;
import resources.partnerapi.requests.ListCustomerCdrsRequest;
import resources.partnerapi.requests.ListCustomerNumbersRequest;
import resources.partnerapi.requests.ListCustomerTransactionsRequest;
import resources.partnerapi.requests.ListKycSessionsRequest;
import resources.partnerapi.requests.PartnerTransferBalanceRequest;
import resources.partnerapi.requests.ResendKycSessionRequest;
import resources.partnerapi.requests.RevokeKycSessionRequest;
import resources.partnerapi.types.GetPartnerDashboardResponse;
import resources.partnerapi.types.GetPartnerProfileResponse;
import resources.partnerapi.types.ListCustomerAccountsResponse;
import resources.partnerapi.types.ListCustomerCdrsResponse;
import resources.partnerapi.types.ListCustomerNumbersResponse;
import resources.partnerapi.types.ListCustomerTransactionsResponse;
import resources.partnerapi.types.ListKycSessionsResponse;

public class AsyncRawPartnerApiClient {
  protected final ClientOptions clientOptions;

  public AsyncRawPartnerApiClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns the authenticated partner's profile and balance.
   */
  public CompletableFuture<VobizApiHttpResponse<GetPartnerProfileResponse>> getPartnerProfile() {
    return getPartnerProfile(null);
  }

  /**
   * Returns the authenticated partner's profile and balance.
   */
  public CompletableFuture<VobizApiHttpResponse<GetPartnerProfileResponse>> getPartnerProfile(
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/partner/me");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      CompletableFuture<VobizApiHttpResponse<GetPartnerProfileResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetPartnerProfileResponse.class), response));
              return;
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (IOException e) {
            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Aggregated partner metrics - total customers, active accounts, balance
     * held across the partner ecosystem, MTD revenue, etc.
     */
    public CompletableFuture<VobizApiHttpResponse<GetPartnerDashboardResponse>> getPartnerDashboard(
        ) {
      return getPartnerDashboard(null);
    }

    /**
     * Aggregated partner metrics - total customers, active accounts, balance
     * held across the partner ecosystem, MTD revenue, etc.
     */
    public CompletableFuture<VobizApiHttpResponse<GetPartnerDashboardResponse>> getPartnerDashboard(
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/partner/dashboard");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<VobizApiHttpResponse<GetPartnerDashboardResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetPartnerDashboardResponse.class), response));
                return;
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (IOException e) {
              future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Returns all customer sub-accounts under your partner account.
       */
      public CompletableFuture<VobizApiHttpResponse<ListCustomerAccountsResponse>> listCustomerAccounts(
          ) {
        return listCustomerAccounts(ListCustomerAccountsRequest.builder().build());
      }

      /**
       * Returns all customer sub-accounts under your partner account.
       */
      public CompletableFuture<VobizApiHttpResponse<ListCustomerAccountsResponse>> listCustomerAccounts(
          RequestOptions requestOptions) {
        return listCustomerAccounts(ListCustomerAccountsRequest.builder().build(),requestOptions);
      }

      /**
       * Returns all customer sub-accounts under your partner account.
       */
      public CompletableFuture<VobizApiHttpResponse<ListCustomerAccountsResponse>> listCustomerAccounts(
          ListCustomerAccountsRequest request) {
        return listCustomerAccounts(request,null);
      }

      /**
       * Returns all customer sub-accounts under your partner account.
       */
      public CompletableFuture<VobizApiHttpResponse<ListCustomerAccountsResponse>> listCustomerAccounts(
          ListCustomerAccountsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/partner/accounts");if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
          }
          if (request.getPerPage().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "per_page", request.getPerPage().get(), false);
          }
          if (request.getSearch().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "search", request.getSearch().get(), false);
          }
          if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          CompletableFuture<VobizApiHttpResponse<ListCustomerAccountsResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCustomerAccountsResponse.class), response));
                  return;
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (IOException e) {
                future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Creates a new customer sub-account under your partner account. VoBiz
         * emails the customer their login credentials and (separately) a KYC link
         * via the kyc-sessions endpoint.
         */
        public CompletableFuture<VobizApiHttpResponse<Object>> createCustomerAccount(
            CreateCustomerAccountRequest request) {
          return createCustomerAccount(request,null);
        }

        /**
         * Creates a new customer sub-account under your partner account. VoBiz
         * emails the customer their login credentials and (separately) a KYC link
         * via the kyc-sessions endpoint.
         */
        public CompletableFuture<VobizApiHttpResponse<Object>> createCustomerAccount(
            CreateCustomerAccountRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/partner/accounts");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VobizApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            CompletableFuture<VobizApiHttpResponse<Object>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (IOException e) {
                  future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Atomically debits your partner master balance and credits the customer's
           * wallet. Both legs are recorded in each account's ledger. Transfers are
           * <strong>permanent and cannot be reversed.</strong>
           */
          public CompletableFuture<VobizApiHttpResponse<Object>> partnerTransferBalance(
              String customerAuthId, PartnerTransferBalanceRequest request) {
            return partnerTransferBalance(customerAuthId,request,null);
          }

          /**
           * Atomically debits your partner master balance and credits the customer's
           * wallet. Both legs are recorded in each account's ledger. Transfers are
           * <strong>permanent and cannot be reversed.</strong>
           */
          public CompletableFuture<VobizApiHttpResponse<Object>> partnerTransferBalance(
              String customerAuthId, PartnerTransferBalanceRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/v1/partner/accounts")
              .addPathSegment(customerAuthId)
              .addPathSegments("transfer-balance");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
              }
              catch(JsonProcessingException e) {
                throw new VobizApiException("Failed to serialize request", e);
              }
              Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              CompletableFuture<VobizApiHttpResponse<Object>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                    try {
                      if (response.code() == 400) {
                        future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                }
              });
              return future;
            }

            /**
             * Returns the customer's transaction ledger. Filter by date range or
             * transaction type. Useful for billing reconciliation.
             */
            public CompletableFuture<VobizApiHttpResponse<ListCustomerTransactionsResponse>> listCustomerTransactions(
                String customerAuthId) {
              return listCustomerTransactions(customerAuthId,ListCustomerTransactionsRequest.builder().build());
            }

            /**
             * Returns the customer's transaction ledger. Filter by date range or
             * transaction type. Useful for billing reconciliation.
             */
            public CompletableFuture<VobizApiHttpResponse<ListCustomerTransactionsResponse>> listCustomerTransactions(
                String customerAuthId, RequestOptions requestOptions) {
              return listCustomerTransactions(customerAuthId,ListCustomerTransactionsRequest.builder().build(),requestOptions);
            }

            /**
             * Returns the customer's transaction ledger. Filter by date range or
             * transaction type. Useful for billing reconciliation.
             */
            public CompletableFuture<VobizApiHttpResponse<ListCustomerTransactionsResponse>> listCustomerTransactions(
                String customerAuthId, ListCustomerTransactionsRequest request) {
              return listCustomerTransactions(customerAuthId,request,null);
            }

            /**
             * Returns the customer's transaction ledger. Filter by date range or
             * transaction type. Useful for billing reconciliation.
             */
            public CompletableFuture<VobizApiHttpResponse<ListCustomerTransactionsResponse>> listCustomerTransactions(
                String customerAuthId, ListCustomerTransactionsRequest request,
                RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("api/v1/partner/accounts")
                .addPathSegment(customerAuthId)
                .addPathSegments("transactions");if (request.getFromDate().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "from_date", request.getFromDate().get(), false);
                }
                if (request.getToDate().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "to_date", request.getToDate().get(), false);
                }
                if (request.getTransactionType().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "transaction_type", request.getTransactionType().get(), false);
                }
                if (request.getPage().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
                }
                if (request.getPerPage().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "per_page", request.getPerPage().get(), false);
                }
                if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("GET", null)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                CompletableFuture<VobizApiHttpResponse<ListCustomerTransactionsResponse>> future = new CompletableFuture<>();
                client.newCall(okhttpRequest).enqueue(new Callback() {
                  @Override
                  public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                    try (ResponseBody responseBody = response.body()) {
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCustomerTransactionsResponse.class), response));
                        return;
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                      return;
                    }
                    catch (IOException e) {
                      future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                    }
                  }

                  @Override
                  public void onFailure(@NotNull Call call, @NotNull IOException e) {
                    future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                  }
                });
                return future;
              }

              /**
               * Look up any customer's call history. Same filter set as the
               * customer-side CDR endpoint.
               */
              public CompletableFuture<VobizApiHttpResponse<ListCustomerCdrsResponse>> listCustomerCdrs(
                  String customerAuthId) {
                return listCustomerCdrs(customerAuthId,ListCustomerCdrsRequest.builder().build());
              }

              /**
               * Look up any customer's call history. Same filter set as the
               * customer-side CDR endpoint.
               */
              public CompletableFuture<VobizApiHttpResponse<ListCustomerCdrsResponse>> listCustomerCdrs(
                  String customerAuthId, RequestOptions requestOptions) {
                return listCustomerCdrs(customerAuthId,ListCustomerCdrsRequest.builder().build(),requestOptions);
              }

              /**
               * Look up any customer's call history. Same filter set as the
               * customer-side CDR endpoint.
               */
              public CompletableFuture<VobizApiHttpResponse<ListCustomerCdrsResponse>> listCustomerCdrs(
                  String customerAuthId, ListCustomerCdrsRequest request) {
                return listCustomerCdrs(customerAuthId,request,null);
              }

              /**
               * Look up any customer's call history. Same filter set as the
               * customer-side CDR endpoint.
               */
              public CompletableFuture<VobizApiHttpResponse<ListCustomerCdrsResponse>> listCustomerCdrs(
                  String customerAuthId, ListCustomerCdrsRequest request,
                  RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("api/v1/partner/accounts")
                  .addPathSegment(customerAuthId)
                  .addPathSegments("cdrs");if (request.getStartDate().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "start_date", request.getStartDate().get(), false);
                  }
                  if (request.getEndDate().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "end_date", request.getEndDate().get(), false);
                  }
                  if (request.getCallDirection().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "call_direction", request.getCallDirection().get(), false);
                  }
                  if (request.getStatus().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "status", request.getStatus().get(), false);
                  }
                  if (request.getMinDuration().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "min_duration", request.getMinDuration().get(), false);
                  }
                  if (request.getHangupCause().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "hangup_cause", request.getHangupCause().get(), false);
                  }
                  if (request.getPerPage().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "per_page", request.getPerPage().get(), false);
                  }
                  if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  Request.Builder _requestBuilder = new Request.Builder()
                    .url(httpUrl.build())
                    .method("GET", null)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Accept", "application/json");
                  Request okhttpRequest = _requestBuilder.build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  CompletableFuture<VobizApiHttpResponse<ListCustomerCdrsResponse>> future = new CompletableFuture<>();
                  client.newCall(okhttpRequest).enqueue(new Callback() {
                    @Override
                    public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                      try (ResponseBody responseBody = response.body()) {
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCustomerCdrsResponse.class), response));
                          return;
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                        return;
                      }
                      catch (IOException e) {
                        future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                      }
                    }

                    @Override
                    public void onFailure(@NotNull Call call, @NotNull IOException e) {
                      future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                    }
                  });
                  return future;
                }

                /**
                 * Phone numbers currently assigned to a customer account.
                 */
                public CompletableFuture<VobizApiHttpResponse<ListCustomerNumbersResponse>> listCustomerNumbers(
                    String customerAuthId) {
                  return listCustomerNumbers(customerAuthId,ListCustomerNumbersRequest.builder().build());
                }

                /**
                 * Phone numbers currently assigned to a customer account.
                 */
                public CompletableFuture<VobizApiHttpResponse<ListCustomerNumbersResponse>> listCustomerNumbers(
                    String customerAuthId, RequestOptions requestOptions) {
                  return listCustomerNumbers(customerAuthId,ListCustomerNumbersRequest.builder().build(),requestOptions);
                }

                /**
                 * Phone numbers currently assigned to a customer account.
                 */
                public CompletableFuture<VobizApiHttpResponse<ListCustomerNumbersResponse>> listCustomerNumbers(
                    String customerAuthId, ListCustomerNumbersRequest request) {
                  return listCustomerNumbers(customerAuthId,request,null);
                }

                /**
                 * Phone numbers currently assigned to a customer account.
                 */
                public CompletableFuture<VobizApiHttpResponse<ListCustomerNumbersResponse>> listCustomerNumbers(
                    String customerAuthId, ListCustomerNumbersRequest request,
                    RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                    .addPathSegments("api/v1/partner/accounts")
                    .addPathSegment(customerAuthId)
                    .addPathSegments("numbers");if (request.getSearch().isPresent()) {
                      QueryStringMapper.addQueryParameter(httpUrl, "search", request.getSearch().get(), false);
                    }
                    if (request.getPage().isPresent()) {
                      QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
                    }
                    if (request.getPerPage().isPresent()) {
                      QueryStringMapper.addQueryParameter(httpUrl, "per_page", request.getPerPage().get(), false);
                    }
                    if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    Request.Builder _requestBuilder = new Request.Builder()
                      .url(httpUrl.build())
                      .method("GET", null)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Accept", "application/json");
                    Request okhttpRequest = _requestBuilder.build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    CompletableFuture<VobizApiHttpResponse<ListCustomerNumbersResponse>> future = new CompletableFuture<>();
                    client.newCall(okhttpRequest).enqueue(new Callback() {
                      @Override
                      public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                        try (ResponseBody responseBody = response.body()) {
                          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                          if (response.isSuccessful()) {
                            future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCustomerNumbersResponse.class), response));
                            return;
                          }
                          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                          future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                          return;
                        }
                        catch (IOException e) {
                          future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                        }
                      }

                      @Override
                      public void onFailure(@NotNull Call call, @NotNull IOException e) {
                        future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                      }
                    });
                    return future;
                  }

                  /**
                   * Returns the authenticated partner's KYC sessions. Filter the list by
                   * session status or customer account, and use <code>page</code> and <code>size</code> to
                   * paginate the results.
                   */
                  public CompletableFuture<VobizApiHttpResponse<ListKycSessionsResponse>> listKycSessions(
                      ) {
                    return listKycSessions(ListKycSessionsRequest.builder().build());
                  }

                  /**
                   * Returns the authenticated partner's KYC sessions. Filter the list by
                   * session status or customer account, and use <code>page</code> and <code>size</code> to
                   * paginate the results.
                   */
                  public CompletableFuture<VobizApiHttpResponse<ListKycSessionsResponse>> listKycSessions(
                      RequestOptions requestOptions) {
                    return listKycSessions(ListKycSessionsRequest.builder().build(),requestOptions);
                  }

                  /**
                   * Returns the authenticated partner's KYC sessions. Filter the list by
                   * session status or customer account, and use <code>page</code> and <code>size</code> to
                   * paginate the results.
                   */
                  public CompletableFuture<VobizApiHttpResponse<ListKycSessionsResponse>> listKycSessions(
                      ListKycSessionsRequest request) {
                    return listKycSessions(request,null);
                  }

                  /**
                   * Returns the authenticated partner's KYC sessions. Filter the list by
                   * session status or customer account, and use <code>page</code> and <code>size</code> to
                   * paginate the results.
                   */
                  public CompletableFuture<VobizApiHttpResponse<ListKycSessionsResponse>> listKycSessions(
                      ListKycSessionsRequest request, RequestOptions requestOptions) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                      .addPathSegments("api/v1/partner/kyc-sessions");if (request.getStatus().isPresent()) {
                        QueryStringMapper.addQueryParameter(httpUrl, "status", request.getStatus().get(), false);
                      }
                      if (request.getAccountAuthId().isPresent()) {
                        QueryStringMapper.addQueryParameter(httpUrl, "account_auth_id", request.getAccountAuthId().get(), false);
                      }
                      if (request.getPage().isPresent()) {
                        QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
                      }
                      if (request.getSize().isPresent()) {
                        QueryStringMapper.addQueryParameter(httpUrl, "size", request.getSize().get(), false);
                      }
                      if (requestOptions != null) {
                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                          httpUrl.addQueryParameter(_key, _value);
                        } );
                      }
                      Request.Builder _requestBuilder = new Request.Builder()
                        .url(httpUrl.build())
                        .method("GET", null)
                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                        .addHeader("Accept", "application/json");
                      Request okhttpRequest = _requestBuilder.build();
                      OkHttpClient client = clientOptions.httpClient();
                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                        client = clientOptions.httpClientWithTimeout(requestOptions);
                      }
                      CompletableFuture<VobizApiHttpResponse<ListKycSessionsResponse>> future = new CompletableFuture<>();
                      client.newCall(okhttpRequest).enqueue(new Callback() {
                        @Override
                        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                          try (ResponseBody responseBody = response.body()) {
                            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                            if (response.isSuccessful()) {
                              future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListKycSessionsResponse.class), response));
                              return;
                            }
                            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                            future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                            return;
                          }
                          catch (IOException e) {
                            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                          }
                        }

                        @Override
                        public void onFailure(@NotNull Call call, @NotNull IOException e) {
                          future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                        }
                      });
                      return future;
                    }

                    /**
                     * Triggers VoBiz to email a KYC link to the customer. KYC is OTP-based
                     * (PAN + Aadhaar via DigiLocker for individuals, PAN + GSTIN for
                     * companies). No document uploads required.
                     */
                    public CompletableFuture<VobizApiHttpResponse<Object>> createKycSession(
                        CreateKycSessionRequest request) {
                      return createKycSession(request,null);
                    }

                    /**
                     * Triggers VoBiz to email a KYC link to the customer. KYC is OTP-based
                     * (PAN + Aadhaar via DigiLocker for individuals, PAN + GSTIN for
                     * companies). No document uploads required.
                     */
                    public CompletableFuture<VobizApiHttpResponse<Object>> createKycSession(
                        CreateKycSessionRequest request, RequestOptions requestOptions) {
                      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                        .addPathSegments("api/v1/partner/kyc-sessions");if (requestOptions != null) {
                          requestOptions.getQueryParameters().forEach((_key, _value) -> {
                            httpUrl.addQueryParameter(_key, _value);
                          } );
                        }
                        RequestBody body;
                        try {
                          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                        }
                        catch(JsonProcessingException e) {
                          throw new VobizApiException("Failed to serialize request", e);
                        }
                        Request okhttpRequest = new Request.Builder()
                          .url(httpUrl.build())
                          .method("POST", body)
                          .headers(Headers.of(clientOptions.headers(requestOptions)))
                          .addHeader("Content-Type", "application/json")
                          .addHeader("Accept", "application/json")
                          .build();
                        OkHttpClient client = clientOptions.httpClient();
                        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                          client = clientOptions.httpClientWithTimeout(requestOptions);
                        }
                        CompletableFuture<VobizApiHttpResponse<Object>> future = new CompletableFuture<>();
                        client.newCall(okhttpRequest).enqueue(new Callback() {
                          @Override
                          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                            try (ResponseBody responseBody = response.body()) {
                              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                              if (response.isSuccessful()) {
                                future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                              }
                              try {
                                switch (response.code()) {
                                  case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                  case 422:future.completeExceptionally(new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                }
                              }
                              catch (JsonProcessingException ignored) {
                                // unable to map error response, throwing generic error
                              }
                              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                              future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                              return;
                            }
                            catch (IOException e) {
                              future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                            }
                          }

                          @Override
                          public void onFailure(@NotNull Call call, @NotNull IOException e) {
                            future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                          }
                        });
                        return future;
                      }

                      /**
                       * Returns the current status and available details for one KYC session
                       * owned by the authenticated partner.
                       */
                      public CompletableFuture<VobizApiHttpResponse<Void>> getKycSession(
                          String sessionId) {
                        return getKycSession(sessionId,GetKycSessionRequest.builder().build());
                      }

                      /**
                       * Returns the current status and available details for one KYC session
                       * owned by the authenticated partner.
                       */
                      public CompletableFuture<VobizApiHttpResponse<Void>> getKycSession(
                          String sessionId, RequestOptions requestOptions) {
                        return getKycSession(sessionId,GetKycSessionRequest.builder().build(),requestOptions);
                      }

                      /**
                       * Returns the current status and available details for one KYC session
                       * owned by the authenticated partner.
                       */
                      public CompletableFuture<VobizApiHttpResponse<Void>> getKycSession(
                          String sessionId, GetKycSessionRequest request) {
                        return getKycSession(sessionId,request,null);
                      }

                      /**
                       * Returns the current status and available details for one KYC session
                       * owned by the authenticated partner.
                       */
                      public CompletableFuture<VobizApiHttpResponse<Void>> getKycSession(
                          String sessionId, GetKycSessionRequest request,
                          RequestOptions requestOptions) {
                        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                          .addPathSegments("api/v1/partner/kyc-sessions")
                          .addPathSegment(sessionId);if (requestOptions != null) {
                            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                              httpUrl.addQueryParameter(_key, _value);
                            } );
                          }
                          Request.Builder _requestBuilder = new Request.Builder()
                            .url(httpUrl.build())
                            .method("GET", null)
                            .headers(Headers.of(clientOptions.headers(requestOptions)));
                          Request okhttpRequest = _requestBuilder.build();
                          OkHttpClient client = clientOptions.httpClient();
                          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                            client = clientOptions.httpClientWithTimeout(requestOptions);
                          }
                          CompletableFuture<VobizApiHttpResponse<Void>> future = new CompletableFuture<>();
                          client.newCall(okhttpRequest).enqueue(new Callback() {
                            @Override
                            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                              try (ResponseBody responseBody = response.body()) {
                                if (response.isSuccessful()) {
                                  future.complete(new VobizApiHttpResponse<>(null, response));
                                  return;
                                }
                                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                return;
                              }
                              catch (IOException e) {
                                future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                              }
                            }

                            @Override
                            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                              future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                            }
                          });
                          return future;
                        }

                        /**
                         * Cancels an outstanding KYC session. Customer can no longer use the link.
                         */
                        public CompletableFuture<VobizApiHttpResponse<Object>> revokeKycSession(
                            String sessionId) {
                          return revokeKycSession(sessionId,RevokeKycSessionRequest.builder().build());
                        }

                        /**
                         * Cancels an outstanding KYC session. Customer can no longer use the link.
                         */
                        public CompletableFuture<VobizApiHttpResponse<Object>> revokeKycSession(
                            String sessionId, RequestOptions requestOptions) {
                          return revokeKycSession(sessionId,RevokeKycSessionRequest.builder().build(),requestOptions);
                        }

                        /**
                         * Cancels an outstanding KYC session. Customer can no longer use the link.
                         */
                        public CompletableFuture<VobizApiHttpResponse<Object>> revokeKycSession(
                            String sessionId, RevokeKycSessionRequest request) {
                          return revokeKycSession(sessionId,request,null);
                        }

                        /**
                         * Cancels an outstanding KYC session. Customer can no longer use the link.
                         */
                        public CompletableFuture<VobizApiHttpResponse<Object>> revokeKycSession(
                            String sessionId, RevokeKycSessionRequest request,
                            RequestOptions requestOptions) {
                          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                            .addPathSegments("api/v1/partner/kyc-sessions")
                            .addPathSegment(sessionId);if (requestOptions != null) {
                              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                httpUrl.addQueryParameter(_key, _value);
                              } );
                            }
                            RequestBody body;
                            try {
                              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                            }
                            catch(JsonProcessingException e) {
                              throw new VobizApiException("Failed to serialize request", e);
                            }
                            Request okhttpRequest = new Request.Builder()
                              .url(httpUrl.build())
                              .method("DELETE", body)
                              .headers(Headers.of(clientOptions.headers(requestOptions)))
                              .addHeader("Content-Type", "application/json")
                              .addHeader("Accept", "application/json")
                              .build();
                            OkHttpClient client = clientOptions.httpClient();
                            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                              client = clientOptions.httpClientWithTimeout(requestOptions);
                            }
                            CompletableFuture<VobizApiHttpResponse<Object>> future = new CompletableFuture<>();
                            client.newCall(okhttpRequest).enqueue(new Callback() {
                              @Override
                              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                                try (ResponseBody responseBody = response.body()) {
                                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                  if (response.isSuccessful()) {
                                    future.complete(new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                    return;
                                  }
                                  try {
                                    if (response.code() == 409) {
                                      future.completeExceptionally(new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                    }
                                  }
                                  catch (JsonProcessingException ignored) {
                                    // unable to map error response, throwing generic error
                                  }
                                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                  future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                  return;
                                }
                                catch (IOException e) {
                                  future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                                }
                              }

                              @Override
                              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                                future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                              }
                            });
                            return future;
                          }

                          /**
                           * Re-dispatches the KYC link to the customer. Rate-limited to once per 30 minutes.
                           */
                          public CompletableFuture<VobizApiHttpResponse<Void>> resendKycSession(
                              String sessionId) {
                            return resendKycSession(sessionId,ResendKycSessionRequest.builder().build());
                          }

                          /**
                           * Re-dispatches the KYC link to the customer. Rate-limited to once per 30 minutes.
                           */
                          public CompletableFuture<VobizApiHttpResponse<Void>> resendKycSession(
                              String sessionId, RequestOptions requestOptions) {
                            return resendKycSession(sessionId,ResendKycSessionRequest.builder().build(),requestOptions);
                          }

                          /**
                           * Re-dispatches the KYC link to the customer. Rate-limited to once per 30 minutes.
                           */
                          public CompletableFuture<VobizApiHttpResponse<Void>> resendKycSession(
                              String sessionId, ResendKycSessionRequest request) {
                            return resendKycSession(sessionId,request,null);
                          }

                          /**
                           * Re-dispatches the KYC link to the customer. Rate-limited to once per 30 minutes.
                           */
                          public CompletableFuture<VobizApiHttpResponse<Void>> resendKycSession(
                              String sessionId, ResendKycSessionRequest request,
                              RequestOptions requestOptions) {
                            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                              .addPathSegments("api/v1/partner/kyc-sessions")
                              .addPathSegment(sessionId)
                              .addPathSegments("resend");if (requestOptions != null) {
                                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                  httpUrl.addQueryParameter(_key, _value);
                                } );
                              }
                              Request.Builder _requestBuilder = new Request.Builder()
                                .url(httpUrl.build())
                                .method("POST", RequestBody.create("", null))
                                .headers(Headers.of(clientOptions.headers(requestOptions)))
                                .addHeader("Accept", "application/json");
                              Request okhttpRequest = _requestBuilder.build();
                              OkHttpClient client = clientOptions.httpClient();
                              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                client = clientOptions.httpClientWithTimeout(requestOptions);
                              }
                              CompletableFuture<VobizApiHttpResponse<Void>> future = new CompletableFuture<>();
                              client.newCall(okhttpRequest).enqueue(new Callback() {
                                @Override
                                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                                  try (ResponseBody responseBody = response.body()) {
                                    if (response.isSuccessful()) {
                                      future.complete(new VobizApiHttpResponse<>(null, response));
                                      return;
                                    }
                                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                    try {
                                      switch (response.code()) {
                                        case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                        return;
                                        case 409:future.completeExceptionally(new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                        return;
                                        case 429:future.completeExceptionally(new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                        return;
                                      }
                                    }
                                    catch (JsonProcessingException ignored) {
                                      // unable to map error response, throwing generic error
                                    }
                                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                    future.completeExceptionally(new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                    return;
                                  }
                                  catch (IOException e) {
                                    future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                                  }
                                }

                                @Override
                                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                                  future.completeExceptionally(new VobizApiException("Network error executing HTTP request", e));
                                }
                              });
                              return future;
                            }
                          }
