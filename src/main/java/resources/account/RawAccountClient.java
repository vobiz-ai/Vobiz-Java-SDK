/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.account;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import errors.UnauthorizedError;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.account.requests.GetConcurrencyRequest;
import resources.account.types.GetConcurrencyResponse;
import resources.account.types.RetrieveAccountResponse;

public class RawAccountClient {
  protected final ClientOptions clientOptions;

  public RawAccountClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve complete account details including pricing tier and credentials.
   */
  public VobizApiHttpResponse<RetrieveAccountResponse> retrieveAccount() {
    return retrieveAccount(null);
  }

  /**
   * Retrieve complete account details including pricing tier and credentials.
   */
  public VobizApiHttpResponse<RetrieveAccountResponse> retrieveAccount(
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/auth/me");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrieveAccountResponse.class), response);
        }
        try {
          if (response.code() == 401) {
            throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Retrieve the current concurrent call usage and configured limits.
     */
    public VobizApiHttpResponse<GetConcurrencyResponse> getConcurrency(String authId) {
      return getConcurrency(authId,GetConcurrencyRequest.builder().build());
    }

    /**
     * Retrieve the current concurrent call usage and configured limits.
     */
    public VobizApiHttpResponse<GetConcurrencyResponse> getConcurrency(String authId,
        RequestOptions requestOptions) {
      return getConcurrency(authId,GetConcurrencyRequest.builder().build(),requestOptions);
    }

    /**
     * Retrieve the current concurrent call usage and configured limits.
     */
    public VobizApiHttpResponse<GetConcurrencyResponse> getConcurrency(String authId,
        GetConcurrencyRequest request) {
      return getConcurrency(authId,request,null);
    }

    /**
     * Retrieve the current concurrent call usage and configured limits.
     */
    public VobizApiHttpResponse<GetConcurrencyResponse> getConcurrency(String authId,
        GetConcurrencyRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("concurrency");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetConcurrencyResponse.class), response);
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }
    }
