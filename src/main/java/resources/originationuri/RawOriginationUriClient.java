/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.originationuri;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import core.ClientOptions;
import core.MediaTypes;
import core.ObjectMappers;
import core.RequestOptions;
import core.VobizApiApiException;
import core.VobizApiException;
import core.VobizApiHttpResponse;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.originationuri.requests.CreateOriginationUriRequest;
import resources.originationuri.requests.DeleteOriginationUriRequest;
import resources.originationuri.requests.ListOriginationUrisRequest;
import resources.originationuri.requests.UpdateOriginationUriRequest;
import resources.originationuri.types.CreateOriginationUriResponse;
import resources.originationuri.types.ListOriginationUrisResponse;
import resources.originationuri.types.UpdateOriginationUriResponse;

public class RawOriginationUriClient {
  protected final ClientOptions clientOptions;

  public RawOriginationUriClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Add an inbound SIP endpoint (origination URI) to a trunk.
   */
  public VobizApiHttpResponse<CreateOriginationUriResponse> createOriginationUri(String authId,
      CreateOriginationUriRequest request) {
    return createOriginationUri(authId,request,null);
  }

  /**
   * Add an inbound SIP endpoint (origination URI) to a trunk.
   */
  public VobizApiHttpResponse<CreateOriginationUriResponse> createOriginationUri(String authId,
      CreateOriginationUriRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/v1/Account")
      .addPathSegment(authId)
      .addPathSegments("origination-uris");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      RequestBody body;
      try {
        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
      }
      catch(JsonProcessingException e) {
        throw new VobizApiException("Failed to serialize request", e);
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("POST", body)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Content-Type", "application/json")
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateOriginationUriResponse.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VobizApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Retrieve all origination URIs on the account.
     */
    public VobizApiHttpResponse<ListOriginationUrisResponse> listOriginationUris(String authId) {
      return listOriginationUris(authId,ListOriginationUrisRequest.builder().build());
    }

    /**
     * Retrieve all origination URIs on the account.
     */
    public VobizApiHttpResponse<ListOriginationUrisResponse> listOriginationUris(String authId,
        RequestOptions requestOptions) {
      return listOriginationUris(authId,ListOriginationUrisRequest.builder().build(),requestOptions);
    }

    /**
     * Retrieve all origination URIs on the account.
     */
    public VobizApiHttpResponse<ListOriginationUrisResponse> listOriginationUris(String authId,
        ListOriginationUrisRequest request) {
      return listOriginationUris(authId,request,null);
    }

    /**
     * Retrieve all origination URIs on the account.
     */
    public VobizApiHttpResponse<ListOriginationUrisResponse> listOriginationUris(String authId,
        ListOriginationUrisRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/v1/Account")
        .addPathSegment(authId)
        .addPathSegments("trunks")
        .addPathSegments("origination-uris");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListOriginationUrisResponse.class), response);
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VobizApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Update an existing origination URI.
       */
      public VobizApiHttpResponse<UpdateOriginationUriResponse> updateOriginationUri(String authId,
          String uriId, UpdateOriginationUriRequest request) {
        return updateOriginationUri(authId,uriId,request,null);
      }

      /**
       * Update an existing origination URI.
       */
      public VobizApiHttpResponse<UpdateOriginationUriResponse> updateOriginationUri(String authId,
          String uriId, UpdateOriginationUriRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/v1/Account")
          .addPathSegment(authId)
          .addPathSegments("origination-uris")
          .addPathSegment(uriId);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new VobizApiException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("PUT", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateOriginationUriResponse.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VobizApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Delete an origination URI from a trunk.
         */
        public VobizApiHttpResponse<Optional<String>> deleteOriginationUri(String authId,
            String uriId) {
          return deleteOriginationUri(authId,uriId,DeleteOriginationUriRequest.builder().build());
        }

        /**
         * Delete an origination URI from a trunk.
         */
        public VobizApiHttpResponse<Optional<String>> deleteOriginationUri(String authId,
            String uriId, RequestOptions requestOptions) {
          return deleteOriginationUri(authId,uriId,DeleteOriginationUriRequest.builder().build(),requestOptions);
        }

        /**
         * Delete an origination URI from a trunk.
         */
        public VobizApiHttpResponse<Optional<String>> deleteOriginationUri(String authId,
            String uriId, DeleteOriginationUriRequest request) {
          return deleteOriginationUri(authId,uriId,request,null);
        }

        /**
         * Delete an origination URI from a trunk.
         */
        public VobizApiHttpResponse<Optional<String>> deleteOriginationUri(String authId,
            String uriId, DeleteOriginationUriRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/v1/Account")
            .addPathSegment(authId)
            .addPathSegments("origination-uris")
            .addPathSegment(uriId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("DELETE", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new VobizApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<Optional<String>>() {}), response);
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VobizApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VobizApiException("Network error executing HTTP request", e);
            }
          }
        }
