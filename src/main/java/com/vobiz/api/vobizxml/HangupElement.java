package com.vobiz.api.vobizxml;

/** {@code <Hangup/>} end/reject the call (self-closing). */
public final class HangupElement extends VobizXmlElement<HangupElement> {

    HangupElement() {
        super("Hangup");
    }
}
