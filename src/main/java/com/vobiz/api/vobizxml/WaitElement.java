package com.vobiz.api.vobizxml;

/** {@code <Wait/>} silent pause (self-closing). */
public final class WaitElement extends VobizXmlElement<WaitElement> {

    WaitElement() {
        super("Wait");
    }
}
