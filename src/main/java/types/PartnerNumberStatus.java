/**
 * This file was auto-generated by Fern from our API Definition.
 */

package types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Object;
import java.lang.String;

public final class PartnerNumberStatus {
  public static final PartnerNumberStatus INACTIVE = new PartnerNumberStatus(Value.INACTIVE, "inactive");

  public static final PartnerNumberStatus EXPIRED = new PartnerNumberStatus(Value.EXPIRED, "expired");

  public static final PartnerNumberStatus ACTIVE = new PartnerNumberStatus(Value.ACTIVE, "active");

  private final Value value;

  private final String string;

  PartnerNumberStatus(Value value, String string) {
    this.value = value;
    this.string = string;
  }

  public Value getEnumValue() {
    return value;
  }

  @java.lang.Override
  @JsonValue
  public String toString() {
    return this.string;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    return (this == other) 
      || (other instanceof PartnerNumberStatus && this.string.equals(((PartnerNumberStatus) other).string));
  }

  @java.lang.Override
  public int hashCode() {
    return this.string.hashCode();
  }

  public <T> T visit(Visitor<T> visitor) {
    switch (value) {
      case INACTIVE:
        return visitor.visitInactive();
      case EXPIRED:
        return visitor.visitExpired();
      case ACTIVE:
        return visitor.visitActive();
      case UNKNOWN:
      default:
        return visitor.visitUnknown(string);
    }
  }

  @JsonCreator(
      mode = JsonCreator.Mode.DELEGATING
  )
  public static PartnerNumberStatus valueOf(String value) {
    switch (value) {
      case "inactive":
        return INACTIVE;
      case "expired":
        return EXPIRED;
      case "active":
        return ACTIVE;
      default:
        return new PartnerNumberStatus(Value.UNKNOWN, value);
    }
  }

  public enum Value {
    ACTIVE,

    INACTIVE,

    EXPIRED,

    UNKNOWN
  }

  public interface Visitor<T> {
    T visitActive();

    T visitInactive();

    T visitExpired();

    T visitUnknown(String unknownType);
  }
}
